# Bartal — Tasks

_Last updated: 2026-05-24T13:24:06Z_

> Living task tracker. Updated every time work starts, completes, blocks, or scope changes.
> Source of truth for "what's done / what's next". See `CLAUDE.md` §"Task Tracking" for the rules.
>
> **Legend:** `[ ]` todo · `[~]` in progress · `[x]` done · `[!]` blocked · `[-]` dropped/deferred

---

## Snapshot

| Surface | Done | Total | Notes |
|---|---:|---:|---|
| Shared package (`@bartal/shared`) | 6 / 6 | ✅ | Builds clean to `dist/` |
| Backend modules (`apps/api`) | 12 / 12 | ✅ | **Phase 2 complete.** All 12 modules implemented: products, categories, delivery, health, auth, users, cart, orders, storage, notifications, reviews, **admin** (198 unit + 9 e2e across 18 suites). Admin module gained `GET /admin/reviews`, `GET /admin/reviews/kpis`, `POST /admin/reviews/:id/{approve,reject,reset}`, `GET /admin/staff`, `GET /admin/audit-log` for Slice 3a moderation. Review schema gained moderation columns (status, flagged_reason, rejection_reason, moderated_by, moderated_at) + auto-flag at customer create. `AuditLog` model + inline writes from every admin mutation, including review actions. |
| Database (`apps/api/prisma`) | 4 / 4 | ✅ | Migration applied, seed run against Postgres :5433. Migration `add_review_moderation` (2026-05-22) added `ReviewModerationStatus` enum + 5 columns on `reviews` + 2 indexes + backfill of existing rows to APPROVED. |
| Mobile screens (`apps/mobile`) | 0 / ~45 | — | Phase 4 not started. Variation: `mobile-v1.jsx` (Marketplace Classic) per CLAUDE.md §5. |
| Web pages (`apps/web`) | 40 / 40 | ✅ | **Phase 5 complete.** Static (8) + checkout (4) + Account+Orders+Wishlist (11) + Journal + Brand (2) + Login/Register/Forgot/Verify-OTP/Reset-Password (5, real backend wired) = 40/40 customer routes. Plus internal `/design-system` (dev-only, `noindex`). Auth glue (Server Actions + httpOnly cookies + middleware refresh + cart sync) + SEO/perf (sitemap, robots, OG, AVIF/WebP, per-page metadata) all shipped. |
| Admin pages (`apps/admin`) | 24 / 24 | ✅ | **Phase 6 Slices 1 + 2 + 3a + 3b-1 + 3b-2 shipped.** Slice 1: Login + Dashboard + Orders + Customers + Zones + Settings (General/Banking). Slice 2: Products + Categories. Slice 3a: AdminReviews + AdminStaff. Slice 3b-1: Analytics + InventoryLog + AbandonedCarts. **Slice 3b-2 (order ops & compliance):** AdminRefunds (admin-initiated `RefundRequest` model with `RFD-YYYY-NNNNN` numbering, approve cascades to Order.status=REFUNDED + payment_status=REFUNDED + SMS via new `ORDER_REFUNDED` template, full refunds only), AdminShippingLabels (new `tracking_number`/`label_printed_at` columns on Order, derives `BTL-…` tracking from `BRT-…`, browser print via `@media print` CSS — no PDF lib), AdminTemplates (read-only viewer for the 10 actually-wired SMS templates, SMS-only channel, AR + EN side-by-side with `{{variable}}` highlighting), plus enabled the previously-disabled AbandonedCarts SMS button via new `CART_ABANDONED` template + `POST /admin/abandoned-carts/:userId/sms` endpoint with 24h server-side rate-limit via audit_log lookup. Sidebar reordered: + Refunds (after Orders), + Shipping labels (after Abandoned carts), + Templates (after Analytics). Only `marketing` remains "Soon". 3 pages remain for Slice 3b-3: promos, banners, settings tabs 3–8. |
| Infrastructure (`infra/`) | 0 / 6 | — | Phase 7 not started. |

> When a `[x]` is in dispute, verify against the actual repo (the file / endpoint / test must exist) before reporting it as done — see CLAUDE.md §9.

---

## Phase 1 — Shared package (`packages/shared`)

- [x] Scaffold `@bartal/shared` (package.json, tsconfig, src layout) — 2026-05-15
- [x] Enums mirroring PRD §9 — 2026-05-15
- [x] Sudan constants: delivery zones, phone +249 regex, SDG formatter, order-number helper — 2026-05-15
- [x] i18n string bundles (AR + EN) per PRD §14.3 categories — 2026-05-15
- [x] Zod schemas: register, login, verifyOtp, address (landmark required), createOrder — 2026-05-15
- [x] Build produces `dist/` — 2026-05-15

## Phase 2 — Backend API (`apps/api`)

### Scaffolding (done)

- [x] NestJS skeleton (main, app module, config, validation, Swagger, global pipe/filter/interceptor) — 2026-05-15
- [x] Prisma + Redis modules wired — 2026-05-15
- [x] Common guards (`JwtAuthGuard`, `AdminGuard`) + decorators (`@Public`, `@CurrentUser`) + utils — 2026-05-15
- [x] All 12 modules scaffolded with route signatures + DTOs + Swagger; service methods throw `NotImplementedException` until implemented — 2026-05-15

### `products` module — done
- [x] List with filters (category, search, in-stock, price range), pagination, sort — 2026-05-15
- [x] Detail by `slug` or `id` (404 + bilingual error) — 2026-05-15
- [x] Search (`q` matches `name_ar` / `name_en` / `description_*`) — 2026-05-15
- [x] Jest unit tests covering service logic + Prisma mocking — 2026-05-15

### `categories` module — done
- [x] Tree endpoint (active categories ordered by `display_order`) — 2026-05-15
- [x] Detail by slug — 2026-05-15
- [x] Products-in-category with pagination — 2026-05-15
- [x] Jest unit tests — 2026-05-15

### `delivery` module — done
- [x] Zones list (Sudan-specific: Khartoum, Omdurman, Bahri, Other) — 2026-05-15
- [x] Fee calculation (zone + free-above-threshold) — 2026-05-15
- [x] Jest unit tests — 2026-05-15

### `auth` module — done (8 endpoints + 24 unit tests + e2e file)
> PRD §10.3 (Auth) + §15. **Acceptance:** all 8 routes return PRD-shaped envelopes, OTP delivery hits Africa's Talking (or logs in dev), refresh-token rotation works, bcrypt-12 confirmed in tests.
- [x] `POST /auth/register` — create user (phone+password+name), bcrypt 12, send OTP, return `{ userId, expiresAt }` — 2026-05-16
- [x] `POST /auth/verify-otp` — verify against `otp_codes`, mark phone verified, issue tokens — 2026-05-16
- [x] `POST /auth/resend-otp` — throttled 5/15min (already wired via `@Throttle`) + per-user OTP issuance cap inside the service — 2026-05-16
- [x] `POST /auth/login` — throttled 10/15min, return access (15m) + refresh (30d); persist `refresh_tokens` hash (sha256) — 2026-05-16
- [x] `POST /auth/refresh` — rotate refresh token, return new pair, atomic CAS via `updateMany({ revoked: false })` — 2026-05-16
- [x] `POST /auth/logout` — invalidate all refresh tokens for user — 2026-05-16
- [x] `POST /auth/forgot-password` — issue reset OTP (generic response for unknown users to avoid enumeration) — 2026-05-16
- [x] `POST /auth/reset-password` — verify OTP + set new bcrypt hash + revoke all refresh tokens in one transaction — 2026-05-16
- [x] e2e: register → verify → login → refresh → logout happy path written at `test/auth.e2e-spec.ts` (requires `pnpm db:up` before running) — 2026-05-16

### `users` module — done (9 endpoints + 20 unit tests)
> PRD §10.3 (Users / Addresses). **Acceptance:** authenticated routes only; address writes reject if `landmark` is empty (matches `@bartal/shared` zod schema).
- [x] `GET /users/me` — profile (returns `PublicUser` shape; 404 on inactive) — 2026-05-16
- [x] `PUT /users/me` — partial update with email-uniqueness check (409 EMAIL_EXISTS) — 2026-05-16
- [x] `POST /users/me/change-password` — bcrypt verify current + rehash + revoke all refresh tokens — 2026-05-16
- [x] `DELETE /users/me` — soft-delete via `is_active = false` + revoke all refresh tokens — 2026-05-16
- [x] `GET /users/me/addresses` — list (default first, then newest) — 2026-05-16
- [x] `POST /users/me/addresses` — create; first address auto-promotes to default; `is_default: true` unsets others atomically — 2026-05-16
- [x] `PUT /users/me/addresses/:id` — update with ownership-safe `findFirst` lookup; promotes to default atomically when toggled — 2026-05-16
- [x] `DELETE /users/me/addresses/:id` — delete with ownership check; P2003 (FK from orders) → 409 `ADDRESS_HAS_ORDERS` — 2026-05-16
- [x] `PUT /users/me/addresses/:id/default` — atomic unset-others + set-this transaction — 2026-05-16

### `cart` module — done (5 endpoints + 16 unit tests)
> PRD §7.1.3 + §10.3 (Cart). Redis-backed (`bartal:cart:<userId>`, 30-day TTL). **Acceptance:** cart survives token refresh, items expire after 30 days, quantity capped at stock.
- [x] `GET /cart` — recomputes the `CartView` from Redis + live Product join, attaches delivery preview from user's default address — 2026-05-16
- [x] `POST /cart/items` — validates active product + stock; merges with existing line by summing; 409 `OUT_OF_STOCK` if merged qty > stock — 2026-05-16
- [x] `PUT /cart/items/:productId` — replaces quantity; 404 `ITEM_NOT_IN_CART` / 409 `OUT_OF_STOCK` — 2026-05-16
- [x] `DELETE /cart/items/:productId` — idempotent remove; empty cart → `redis.del` instead of write — 2026-05-16
- [x] `DELETE /cart` — `redis.del` + return empty view — 2026-05-16

### `orders` module — done (5 endpoints + 18 unit tests + e2e file)
> PRD §10.3 (Orders) + §11 (Payment flow). **Acceptance:** order number is `BRT-YYYY-NNNNN` (sequential per year, generated server-side), status transitions follow PRD §11.3 state machine, receipt uploads write a URL (R2-presign is deferred to `storage` module per user decision).
- [x] `GET /orders` — paginated list for current user (page+limit clamped; newest-first; includes items + status_history + address) — 2026-05-20
- [x] `GET /orders/:id` — detail with status timeline (ownership-safe `findFirst({ id, user_id })`) — 2026-05-20
- [x] `POST /orders` — create from validated items + delivery address; interactive `$transaction`: order + items + initial status_history + per-product stock decrement; clears Redis cart; fires bilingual SMS (post-commit isolation so a transient SMS/Redis failure can't roll back) — 2026-05-20
- [x] `DELETE /orders/:id/cancel` — customer-cancel from {PENDING, AWAITING_PAYMENT, RECEIPT_UPLOADED}; stock restored per line; history appended; SMS fired — 2026-05-20
- [x] `POST /orders/:id/receipt` — accepts `receipt_url` (storage presign deferred); transitions AWAITING_PAYMENT|PAYMENT_REJECTED → RECEIPT_UPLOADED; SMS fired — 2026-05-20
- [x] Order-number generator wired with `P2002`-retry-once inside create tx (covers count-based collision window) — 2026-05-20
- [x] State machine in `helpers/state-machine.ts` mirrors PRD §11.3 (`canTransition`, `cancellableStatuses`, `receiptUploadableStatuses`) — 2026-05-20
- [x] SMS triggers on order create / cancel / receipt-uploaded (NotificationsService logs STUBBED in dev) — 2026-05-20

### Product reviews — done (extracted to its own module + 10 unit tests + 3 e2e)
> PRD §7.1.5 + §10.3. **Acceptance:** only buyers of the product (delivered orders) can post; verified-purchase badge stored on the review row.
- [x] `POST /products/:id/reviews` — auth required; verifies the user has a DELIVERED order containing the product; throws 403 NOT_A_BUYER otherwise; `is_verified_purchase=true` always (no non-buyer path); P2002 → 409 REVIEW_ALREADY_EXISTS via the `@@unique([product_id, user_id])` constraint — 2026-05-21
- [x] `GET /products/:id/reviews` — public; paginated (page+limit clamped to 50); `sort ∈ {newest, highest, lowest}`; returns items + summary `{ count, average_rating, distribution: 1..5 }` computed via `groupBy` — 2026-05-21
- [x] Extracted from `ProductsService` into a new `apps/api/src/modules/reviews/` module (service + controller + DTOs + spec). Products controller/service shed the 2 review routes/methods + `CreateReviewDto`. URL paths unchanged. — 2026-05-21
- [-] Admin approve/reject endpoint — deferred to admin slice (auto-publish on insert; no `is_published` column added; admin module will add columns + moderation routes + audit_log writes when it lands)

### `notifications` module — done (real AT + FCM adapters + 13 unit tests + e2e)
> PRD §15 + Sudan constraints (SMS is dominant, FCM secondary, WhatsApp human-driven only). **Acceptance:** every send logged to `sms_logs`, FCM tokens stored on `User.fcm_token`, dev mode no-ops gracefully when keys absent.
- [x] Africa's Talking SMS adapter — raw `fetch` + `apiKey` header (no SDK); sandbox URL when `AT_USERNAME=sandbox`, production URL otherwise; 10s timeout via `AbortController`; status-code mapping (101/102 → SENT, others → FAILED with statusCode in `error_message`) — 2026-05-20
- [x] Firebase Admin SDK FCM adapter — `firebase-admin@^12.7.0`; lazy + safe init in `onModuleInit` (missing path / file / parse / init failure → `fcmStubMode = true` permanently); revoked-token (`messaging/registration-token-not-registered`) clears `User.fcm_token` via `prisma.user.updateMany` — 2026-05-20
- [x] Bilingual templates for OTP (register / login / reset) — already lived in `auth/helpers/messages.ts`; no changes needed — 2026-05-15
- [x] Bilingual templates for customer-callable `OrderStatus` transitions — `orders/helpers/sms-templates.ts` (ORDER_CREATED_BANK / ORDER_CREATED_COD / RECEIPT_RECEIVED / ORDER_CANCELLED) — 2026-05-20
- [-] Admin-driven status templates (PAYMENT_CONFIRMED / SHIPPED / DELIVERED / PAYMENT_REJECTED) — deferred; wired when admin module adds the endpoints
- [x] `sms_logs` write on every send (provider + status + provider_ref + error_message); cost goes to logger only (no schema column this slice) — 2026-05-20
- [x] `PUT /users/me/fcm-token` — auth user; accepts `string | null`; clears on null. Tested in `auth.e2e-spec.ts`. — 2026-05-20
- [-] Retry queue with exponential backoff for transient failures — deferred to Phase 2 per user scope decision (single attempt + FAILED row + admin/operator reads `sms_logs`)

### `storage` module — done (3 endpoints + 13 unit tests + e2e file)
> PRD §15 (R2 / S3-compatible) + §16.4. **Acceptance:** product images public via CDN, receipt images private with 1h signed URLs, all uploads pass through `sharp` → WebP / ≤200KB / ≤1200px.
- [x] `POST /storage/products` — admin only, server-side multipart + sharp pipeline + R2 PUT (public bucket); returns `{ key, url }` — 2026-05-20
- [x] `POST /storage/receipts` — auth user; ownership-safe order lookup; sharp pipeline + R2 PUT (private bucket); returns `{ key }` for the client to pass to `POST /orders/:id/receipt` — 2026-05-20
- [x] `POST /storage/receipts/signed-url` — admin only; 1h signed GET URL via `@aws-sdk/s3-request-presigner` — 2026-05-20
- [x] `sharp` pipeline: WebP, EXIF-rotate, width clamp 1200px, quality 80 → drop to 60 if >200KB, hard-reject above 200KB with `IMAGE_BUDGET_EXCEEDED` — 2026-05-20
- [x] Stub-in-dev mode mirroring `NotificationsService`: when `R2_ACCESS_KEY_ID` empty, sharp still runs but PUT is skipped and keys are prefixed `stub/...` so CI + dev work without real R2 credentials — 2026-05-20
- [-] Image-cleanup worker (delete orphaned uploads >24h old) — deferred to Phase 2 per user scope decision; needs a separate Upload-tracking table or bucket-scan job

### `admin` module — done (17 endpoints + 25 unit tests + 1 admin e2e + audit_log)
> PRD §7.2 + §10.3 (Admin). Guarded by `JwtAuthGuard + AdminGuard`. **Acceptance:** every mutation appended to `audit_log` with `actor_id`, dashboard KPIs match PRD §7.2.2.
- [x] `GET /admin/dashboard` — revenue today + orders-by-status groupBy + low-stock count + pending-payments count + top-5 last-30-days + 30-day revenue trend via raw SQL `date_trunc` — 2026-05-21
- [x] `GET /admin/analytics/sales?from&to` — defaults to last 30 days; rejects ranges > 365 days; raw SQL group-by-day — 2026-05-21
- [x] `GET /admin/analytics/products?limit` — clamped [1, 50] — 2026-05-21
- [x] `GET /admin/orders?status&zone&from&to&q&page&limit` — filterable + paginated, lighter shape than customer-facing `OrderView` — 2026-05-21
- [x] `PUT /admin/orders/:id/status` — delegates to new `OrdersService.adminUpdateStatus` (state-machine guard + `INVALID_ADMIN_TRANSITION` for receipt-to-payment pairs that must use the payment endpoint + stock restore on admin cancel + SMS via `ORDER_SHIPPED/DELIVERED/CANCELLED` events) — 2026-05-21
- [x] `PUT /admin/orders/:id/payment` — `PAID` → `OrdersService.adminConfirmPayment` (sets `paid_at`, fires `PAYMENT_CONFIRMED` SMS); non-PAID with reason → `adminRejectPayment` (reason ≥3 chars, fires `PAYMENT_REJECTED` SMS with reason; `payment_status` stays UNPAID) — 2026-05-21
- [x] `POST /admin/products` — bilingual fields required, P2002 slug/sku → 409, audit `CREATE` — 2026-05-21
- [x] `PUT /admin/products/:id` — partial update + P2002 slug → 409 + audit `UPDATE` with before/after — 2026-05-21
- [x] `DELETE /admin/products/:id` — soft delete via `is_active=false`; 409 `PRODUCT_ALREADY_DELETED` if double-call — 2026-05-21
- [x] `POST /admin/products/:id/images` — multipart `FileInterceptor` + `imageMimeFilter`; delegates to `StorageService.uploadProductImage` (sharp → WebP → R2); primary flag de-duplicates prior primary in tx — 2026-05-21
- [x] `POST /admin/categories` + `PUT /admin/categories/:id` — bilingual; parent_id existence check; cycle detection (depth 5) — 2026-05-21
- [x] `GET /admin/customers?q&page&limit` — paginated, includes `order_count` + `last_order_at` (single `groupBy` per page; no N+1) — 2026-05-21
- [x] `GET /admin/customers/:id` — detail with addresses, last 10 orders, `total_spent` via aggregate — 2026-05-21
- [x] `PUT /admin/delivery/zones/:zone` — fee + ETA + audit `ZONE_FEE_UPDATE` — 2026-05-21
- [x] `GET /admin/settings` + `PUT /admin/settings` — batch upsert; per-key validation (regex + 4KB cap + ≤50 keys/call); single summary `SETTINGS_UPDATE` audit row — 2026-05-21
- [x] AuditLog Prisma model + inline writes from each admin handler (try/catch, never blocks response) — 2026-05-21
- [x] Admin-driven SMS templates added to `orders/helpers/sms-templates.ts`: PAYMENT_CONFIRMED / PAYMENT_REJECTED / ORDER_SHIPPED / ORDER_DELIVERED — 2026-05-21
- [-] Bulk product import (CSV → batch create) — deferred to Phase 2 per user scope decision (not in PRD §7.2 / §10.3)

### Cross-cutting
- [x] e2e tests per module — auth, orders, storage, reviews, admin (5 suites; 9 tests) — 2026-05-21
- [x] Audit log writes on admin mutations (inline, not middleware — `before`/`after` snapshots accurate; try/catch never blocks request) — 2026-05-21
- [ ] Rate limit tuning per route (auth already throttled)
- [ ] OpenAPI / Swagger annotations reviewed for every implemented endpoint

## Phase 3 — Database (`apps/api/prisma`)

- [x] Schema mirroring PRD §9 (16 models + 7 enums) — 2026-05-15
- [x] Seed: admin user + 4 delivery zones + 7 app settings + 6 categories + 20 sample Sudan-market products — 2026-05-15
- [x] Run `prisma migrate dev --name init` against local Postgres (port 5433) — 2026-05-15
- [x] Run seed against local DB — 2026-05-15

## Phase 4 — Mobile app (`apps/mobile`)

> **Variation:** `mobile-v1.jsx` (Marketplace Classic — dense grid + bottom tabs) per CLAUDE.md §5. Default fonts: Cairo (AR), Poppins (EN). RTL via `EdgeInsetsDirectional` / `AlignmentDirectional` — never `EdgeInsets.only(left:)`.

### Scaffold + foundations
- [ ] Flutter 3.x project with `pnpm` excluded (Flutter sits outside the workspace per CLAUDE.md §3)
- [ ] Riverpod 2.x + go_router + `flutter_secure_storage` + `dio` + `flutter_localizations`
- [ ] Design tokens module (mirror `apps/web/src/design/tokens.ts` → Dart `BartalColors`, `BartalRadii`, `BartalSpacing`)
- [ ] Cairo + Poppins via `pubspec` assets, scaled `TextTheme`
- [ ] `MotifTile` + `MotifBg` ported to a Flutter `CustomPainter`
- [ ] API client with token refresh interceptor
- [ ] L10n: AR (`ar.arb`) + EN (`en.arb`) — keys mirror `@bartal/shared` STR table

### Auth flow (8 screens)
> Source: `auth-screens.jsx`, `auth-gaps.jsx`
- [ ] `SplashScreen` — `auth-gaps.jsx::SplashScreen`
- [ ] `OnboardingScreen` (3 slides) — `auth-gaps.jsx::OnboardingScreen`
- [ ] `WelcomeScreen` — `auth-screens.jsx::WelcomeScreen`
- [ ] `LoginScreen` — `auth-screens.jsx::LoginScreen`
- [ ] `SignupScreen` — `auth-screens.jsx::SignupScreen`
- [ ] `OtpScreen` — `auth-screens.jsx::OtpScreen`
- [ ] `ForgotPasswordScreen` — `auth-gaps.jsx::ForgotPasswordScreen`
- [ ] `ResetPasswordScreen` — `auth-gaps.jsx::ResetPasswordScreen`

### Browse + product (5 screens)
- [ ] Home with hero + categories + featured grid — `mobile-v1.jsx::V1Home`
- [ ] Product detail (gallery + bilingual desc + add-to-cart + WhatsApp share) — `mobile-v1.jsx::V1Detail`
- [ ] Categories grid — `final-additions.jsx::MobileCategoriesScreen`
- [ ] PDP reviews tab — `final-additions.jsx::MobilePdpReviewsScreen`
- [ ] Bottom tab bar (5 tabs) — `mobile-v1.jsx::V1TabBar`

### Search + discovery (3 screens)
- [ ] Search results — `mobile-extras.jsx::SearchResultsScreen`
- [ ] Filters bottom sheet — `mobile-extras.jsx::FiltersSheet`
- [ ] Wishlist — `mobile-extras.jsx::WishlistScreen`

### Cart + checkout (8 screens)
- [ ] Cart — `secondary-screens.jsx::CartScreen`
- [ ] Cart with promo applied — `final-additions.jsx::MobileCartPromoScreen`
- [ ] Checkout stepper shell — `checkout-flow.jsx::CheckoutStepper`
- [ ] Checkout address step — `checkout-flow.jsx::CheckoutAddressScreen`
- [ ] Checkout payment method step — `checkout-flow.jsx::CheckoutPaymentScreen`
- [ ] Checkout bank-transfer details step — `checkout-flow.jsx::CheckoutBankScreen`
- [ ] Checkout review step — `checkout-flow.jsx::CheckoutReviewScreen`
- [ ] Order confirmation — `secondary-screens.jsx::ConfirmScreen`

### Orders + receipt flow (5 screens)
- [ ] Orders list — `secondary-screens.jsx::OrdersScreen`
- [ ] Order detail — `receipt-flow.jsx::OrderDetailScreen`
- [ ] Upload receipt (camera + gallery + WebP compress before send) — `receipt-flow.jsx::UploadReceiptScreen`
- [ ] Receipt submitted ack — `receipt-flow.jsx::ReceiptSubmittedScreen`
- [ ] Receipt rejected state — `mobile-extras.jsx::ReceiptRejectedScreen`
- [ ] Order tracking timeline — `receipt-flow.jsx::TrackingScreen`
- [ ] Write review — `order-thanks-and-reviews.jsx::MobileWriteReview`

### Profile + settings (6 screens)
- [ ] Profile dashboard — `profile-flow.jsx::ProfileScreen`
- [ ] Edit profile — `mobile-extras.jsx::EditProfileScreen`
- [ ] Change password — `mobile-extras.jsx::ChangePasswordScreen`
- [ ] Addresses list — `profile-flow.jsx::AddressesScreen`
- [ ] Add / edit address (landmark required) — `profile-flow.jsx::AddAddressScreen`
- [ ] Settings (lang, theme, notifications) — `profile-flow.jsx::SettingsScreen`

### Notifications + help (3 screens)
- [ ] Notifications inbox — `mobile-extras.jsx::NotificationsScreen`
- [ ] Help / FAQ — `mobile-extras.jsx::HelpFaqScreen`
- [ ] FCM background handler + deep-link routing

### System states (5 screens)
> Source: `system-kit.jsx`
- [ ] Empty states (cart / wishlist / orders / search / notifications) — `system-kit.jsx::MobileEmptyState`
- [ ] Skeleton states (home / list / detail) — `system-kit.jsx::MobileSkeletonScreen`
- [ ] Error / offline screens — `system-kit.jsx::MobileErrorScreen`
- [ ] Offline banner + connectivity listener (cart survives drops per PRD §7.1.3)

### Ship
- [ ] Widget tests for critical flows (auth, add-to-cart, place-order)
- [ ] Integration test: cold-start → browse → add → checkout (mock API)
- [ ] Android build signed + uploaded to Play Console internal track
- [ ] iOS build via TestFlight (lower priority — ~80% Android share)

## Phase 5 — Website (`apps/web`)

### Done — 2026-05-15
- [x] Next.js 14 scaffold (App Router, src dir, Tailwind, custom locale routing)
- [x] Design tokens materialised from PRD §13 + Claude design `tokens.jsx` (CSS vars + Tailwind preset)
- [x] Cairo + Poppins + JetBrains Mono via `next/font/google`
- [x] Locale routing `[locale]` with middleware-driven SSR `lang`/`dir`
- [x] AR + EN dictionaries (web copy) consuming `@bartal/shared` strings
- [x] Core components: `BartalLogo`, `PriceTag`, `ProductPlaceholder`, `AppButton`, `AppCard`, `StatusBadge`, `LoadingSkeleton`, `EmptyState`, `ErrorState`, `OfflineBanner`, `LanguageToggle`, `Header`, `Footer`, `Icons`
- [x] Live data: `useProducts` / `useCategories` / `useDeliveryZones` (axios + React Query)
- [x] Zustand cart store with localStorage persistence (offline-resilient per PRD §7.1.3)
- [x] Pages: home, products list (filter+sort+paginate), product detail (add-to-cart, WhatsApp), category redirect, cart (zone-aware delivery + free-above), checkout (preview), login/register/forgot-password (UI only), account/orders/wishlist (login-required state), search, 404
- [x] Build passes (`pnpm --filter @bartal/web build` → 18 routes) and prod server serves real seed data on both locales

### Checkout — multi-step flow
> Source: `web-checkout-steps.jsx`. **Acceptance:** stepper persists across reload, address picker re-uses logged-in user's saved addresses, bank-transfer page exposes `wa.me` deep link to support.
- [x] `/checkout/address` (step 1) — `web-checkout-steps.jsx::WebCheckoutAddress` — 2026-05-15
- [-] `/checkout/payment` (step 2) — folded into `/checkout/bank` per design (bank is the only non-COD method); the original `/checkout` preview retained until step 3 ships
- [x] `/checkout/bank` (step 2) — `web-checkout-steps.jsx::WebCheckoutBank` — 2026-05-15
- [x] `/checkout/review` (step 3) — `web-checkout-steps.jsx::WebCheckoutReview` — 2026-05-15
- [x] `/checkout/thank-you/:orderId` — `order-thanks-and-reviews.jsx::WebOrderConfirm` (motif background) — 2026-05-15

### Account
> Source: `profile-flow.jsx::WebAccount*` + `web-extras.jsx` + `web-pages.jsx::WebAccount`. **Acceptance:** sidebar layout matches `WebAccountLayout`; all sub-pages share the same shell.
- [x] `/account` hub — `web-pages.jsx::WebAccount` wrapped in `profile-flow.jsx::WebAccountLayout` (dashboard body: stats + alert + recent orders table) — 2026-05-16
- [x] `/account/profile` — `profile-flow.jsx::WebAccountProfile` (personal info form + verification rows + danger zone) — 2026-05-16
- [x] `/account/addresses` — `profile-flow.jsx::WebAccountAddresses` (landmark required) — 2026-05-16
- [x] `/account/security` — `profile-flow.jsx::WebAccountSecurity` (password + 2FA + active sessions) — 2026-05-16
- [x] `/account/preferences` — **constructed from system-kit primitives** (no design source for the nav item); reuses `LanguageToggle`, theme radio cards, notification toggles — 2026-05-16
- [x] `/orders` — `web-extras.jsx::WebOrderHistory` (replace gated stub) — 2026-05-16
- [x] `/orders/:id` — `web-extras.jsx::WebOrderDetail` — 2026-05-16
- [x] `/orders/:id/receipt-upload` — `web-extras.jsx::WebReceiptUpload` (motif-amber dropzone, drag-and-drop + checklist) — 2026-05-16
- [x] `/orders/:id/review` — `order-thanks-and-reviews.jsx::WebWriteReview` (star rating, tag pills, photo slots, anonymous toggle) — 2026-05-16
- [x] `/orders/:id/invoice` (print view) — `invoice-and-templates.jsx::WebInvoicePrint` (A4 invoice + packing slip, Tailwind `print:` variants) — 2026-05-16
- [x] `/wishlist` — `web-extras.jsx::WebWishlist` (replace gated stub) — 2026-05-16

### Static / content
- [x] `/about` — `system-kit.jsx::WebAbout` — 2026-05-15
- [x] `/privacy` — `system-kit.jsx::WebPrivacy` — 2026-05-15
- [x] `/terms` — `web-extras.jsx::WebToS` — 2026-05-15
- [x] `/faq` — `web-extras.jsx::WebFaq` — 2026-05-15
- [x] `/contact` — `system-kit.jsx::WebContact` — 2026-05-15
- [x] `/journal` — `final-web-and-styletile.jsx::WebJournal` (featured post + 6-category filter + 6-post grid + newsletter strip with motif) — 2026-05-16
- [x] `/brand` — `final-web-and-styletile.jsx::WebBrandPage` (top breadcrumb + navy hero band with motif + 2-col about/trust metrics + 4-tab nav + 6-product grid with discount ribbons) — 2026-05-16

### System states
- [x] `/500` — `system-kit.jsx::WebSystemState` kind=error — 2026-05-15 (mounted at `app/[locale]/error.tsx` with `reset()` retry)
- [x] `/offline` — `system-kit.jsx::WebSystemState` kind=offline — 2026-05-15 (route `/[locale]/offline`; service-worker auto-redirect is a follow-up when SW lands)
- [x] Branded 404 — `system-kit.jsx::WebSystemState` kind='404' wired into `app/[locale]/not-found.tsx` + catch-all `[locale]/[...rest]/page.tsx` so unmatched URLs render the branded page — 2026-05-15

### Design reference page
- [x] `/design-system` — `final-web-and-styletile.jsx::StyleTile` — 2026-05-15 (internal-only: gated behind `NODE_ENV !== 'production'` + `robots: noindex,nofollow`; shows an "INTERNAL · not exposed to customers" banner at top; not counted in customer page budget)

### Auth glue (after `auth` API lands) — done
- [x] Swap login form to real `POST /auth/login` via Server Action + httpOnly+Secure+SameSite=Lax cookies (`bartal_access` 15m / `bartal_refresh` 30d / `bartal_user` JS-readable user JSON) — 2026-05-21
- [x] Real OTP screen — `[locale]/(account)/verify-otp/page.tsx` + `OtpForm.tsx` (6-digit boxes, paste auto-distribute, 60s resend countdown, REGISTER/LOGIN/RESET purpose dispatch) — 2026-05-21
- [x] Real forgot/reset — `[locale]/(account)/reset-password/page.tsx` + `ResetPasswordForm.tsx`; forgot-password now POSTs to `auth/forgot-password` and redirects to `/verify-otp?purpose=RESET` — 2026-05-21
- [x] Server Action `syncCartAction(localItems)` pushes localStorage cart to `POST /cart/items` after login/verify-otp, then `GET /cart` rehydrates the Zustand store; per-line conflicts (`OUT_OF_STOCK`) surfaced via banner — 2026-05-21
- [x] Middleware auth guard at `apps/web/src/middleware.ts` — protected matcher `(account|orders|wishlist|checkout)`, missing-access → refresh via `POST /auth/refresh` and rewrite cookies, both-missing → redirect to `/login?next=…` — 2026-05-21
- [x] Client `useAuth()` hook reads non-httpOnly `bartal_user` cookie + cross-tab `BroadcastChannel('bartal-auth')` — 2026-05-21

### SEO + performance — done
- [x] Per-page metadata (title/description/canonical, bilingual + hreflang) on root layout, locale layout, home, products list, PDP — 2026-05-21
- [x] `sitemap.xml` (static + dynamic from products/categories, revalidate 86_400, AR+EN alternates) — `apps/web/src/app/sitemap.ts` — 2026-05-21
- [x] `robots.txt` — `apps/web/src/app/robots.ts` — disallows `/account`, `/orders`, `/wishlist`, `/checkout`, `/verify-otp`, `/reset-password`, `/design-system` — 2026-05-21
- [x] OG images via `next/og` ImageResponse — root + per-PDP (navy gradient + amber star + product name + price) — 2026-05-21
- [x] `next.config.mjs` images: `formats: ['image/avif', 'image/webp']` + `minimumCacheTTL: 86400` — 2026-05-21
- [-] Lighthouse mobile score > 80 / LCP < 2.5s on 3G — measured manually after deploy; CI gate deferred to Phase 7 infra
- [-] Image budget enforced (WebP, ≤200KB, ≤1200px) — enforced server-side in `storage` module's sharp pipeline; Next image config now also serves AVIF/WebP for client-rendered remotes

## Phase 6 — Admin dashboard (`apps/admin`)

> Source: `admin-pages.jsx`, `admin-extras.jsx`, `admin-extras2.jsx`, `admin-reviews.jsx`, `final-additions.jsx` (admin pages), `invoice-and-templates.jsx::AdminTemplates`, `receipt-flow.jsx::AdminReceiptViewer`. **Auth model:** access token in memory (no localStorage per CLAUDE.md §4).

### Scaffold — Slice 1 done
- [x] React 18 + Vite + TS strict; Tanstack Table; react-hook-form + zod; recharts; react-zoom-pan-pinch — 2026-05-21
- [x] Admin shell (sidebar + topbar + dark-mode toggle + locale toggle) — `admin-pages.jsx::AdminShell` — 2026-05-21
- [x] Tokens mirrored from `apps/web/src/design/tokens.ts` (inlined in `apps/admin/src/design/tokens.ts` — Vite needs static-ESM; CJS re-export from `@bartal/shared` breaks tree-shaking. `packages/shared/src/design/tokens.ts` materialised as the canonical source; `apps/web/src/design/tokens.ts` re-exports from it) — 2026-05-21
- [x] Auth flow → `POST /auth/login` (in-memory tokens per CLAUDE.md §4; Zustand store without `persist`; refresh-on-401 axios interceptor uses the in-memory refresh token; role-gated to `ADMIN`) — 2026-05-21

### Pages — Slice 1 (operator-critical path) done
- [x] `AdminLogin` — split-panel auth with motif backdrop, react-hook-form + zod (`/login`) — 2026-05-21
- [x] `AdminDashboard` (KPI cards + 14-day revenue area chart + status bar chart + recent orders + top sellers; charts lazy-imported) — `admin-pages.jsx::AdminDashboard` (`/`) — 2026-05-21
- [x] `AdminOrders` list (status tabs + zone/date/q filters + Tanstack Table + URL-driven pagination) — `admin-pages.jsx::AdminOrders` (`/orders`) — 2026-05-21
- [x] `AdminOrderDetail` (status timeline + items + customer/shipping/notes sidebar + state-machine action bar) — `admin-extras2.jsx::AdminOrderDetail` (`/orders/:id`) — 2026-05-21
- [x] `AdminReceiptViewer` with zoom-pan (`react-zoom-pan-pinch`) + match-checker toggles + approve/reject — `receipt-flow.jsx::AdminReceiptViewer` (modal off `/orders/:id`) — 2026-05-21
- [x] `AdminProducts` list — `admin-pages.jsx::AdminProducts` (`/products`) — tabs (all/active/inactive/out-of-stock/featured) wired to backend `counts` payload; category dropdown + debounced q; Tanstack Table with thumbnail/name/slug/category/price/stock(color-coded)/status/edit-link; URL-driven pagination — 2026-05-21
- [x] `AdminProductForm` (create / edit, bilingual fields, image upload) — `admin-extras.jsx::AdminProductForm` (`/products/new`, `/products/:id/edit`) — react-hook-form + zod resolver mirroring backend `CreateProductDto`; slug auto-generate from name_en; numeric `sort_order` per image with set-primary + delete via the new image endpoints; soft-delete confirm + activate-via-toggle; sticky pricing/organization/status sidebar — 2026-05-21
- [x] `AdminCategories` — `admin-extras.jsx::AdminCategories` (`/categories`) — client-side tree from flat `GET /admin/categories` payload (depth-indented, expand/collapse via Set), product_count per node, LIVE/HIDDEN pills; right-rail edit panel (bilingual name + slug + parent + sort_order + active) with self+descendant exclusion from parent dropdown to prevent cycles client-side — 2026-05-21
- [x] `AdminCustomers` list — `admin-pages.jsx::AdminCustomers` (`/customers`) — 2026-05-21
- [x] `AdminCustomerDetail` (orders, addresses) — `admin-extras2.jsx::AdminCustomerDetail` (`/customers/:id`) — 2026-05-21
- [x] `AdminZones` (per-zone fee + ETA editor, inline-editable table; SVG Khartoum map deferred to Slice 2) — `admin-pages.jsx::AdminZones` (`/zones`) — 2026-05-21
- [x] `AdminAnalytics` (sales/products deep charts) — `admin-pages.jsx::AdminAnalytics` (`/analytics`) — KPI strip (Revenue/Orders/AOV computed from existing endpoints) + 7/30/90-day range selector + lazy-loaded stacked area chart with `breakdown=zone` toggle + top-10 sellers list. Reuses `GET /admin/analytics/sales` + `GET /admin/analytics/products`; added optional `breakdown=zone` query param backed by new `dailyRevenueByZone` raw SQL (`date_trunc + addresses.zone` group-by). Conversion KPI dropped (no page-view tracking yet). — 2026-05-23
- [x] `AdminReviews` (moderation) — `admin-reviews.jsx::AdminReviews` (`/reviews`) — KPI strip + 4-tab list (Pending/Flagged/Approved/Rejected) + detail panel with auto-flag banner + internal context + Approve/Reject (chip-driven reason) + Reset; reply composer dropped (no schema column). Backend adds `moderation_status` enum + `flagged_reason`/`rejection_reason`/`moderated_by`/`moderated_at` columns; auto-flag at create scans for URL + 5 spam keywords. Customer-facing review list/summary filters to APPROVED. — 2026-05-22
- [x] `AdminSettings` (all 8 tabs live) — `admin-extras.jsx::AdminSettings` (`/settings`, `/settings/:tab`) — General + Banking (2026-05-21). **Tabs 3–8 shipped 2026-05-24 (Slice 3b-3):** Checkout (5 toggles + payment-methods reorder), Tax (4 fields + 3 toggles, config-only — cart doesn't apply VAT), Locales (read-only AR+EN cards with completion %), Team (alias link to /staff), Legal (table of 6 pages → nested `/settings/legal/:slug` editor with bilingual `<textarea>` + status DRAFT/PUBLISHED), Integrations (7 integration toggles + disabled Configure buttons). ~43 new `AppSetting` keys seeded across Checkout/Tax/Locale/Legal/Integrations groups; all save via existing `PUT /admin/settings`.
- [x] `AdminRefunds` — `admin-extras2.jsx::AdminRefunds` (`/refunds`) — KPI strip (Pending/Approved/Rejected counts) + 4 filter tabs + `+ New refund` button opening a dialog (order_id + amount + reason). Approve cascades atomically (refund APPROVED + Order.status=REFUNDED + payment_status=REFUNDED + OrderStatusHistory + audit + `ORDER_REFUNDED` SMS via existing NotificationsService). Reject is inline reason prompt (no full modal). Backend: new `RefundRequest` Prisma model + `RefundStatus` enum + `RFD-YYYY-NNNNN` number generator (count-based, P2002 retry once). State machine extended for PAYMENT_CONFIRMED|PROCESSING|SHIPPED|DELIVERED|CANCELLED → REFUNDED. Eligibility: payment_status=PAID + status in those 5 + no PENDING/APPROVED refund already exists (409 REFUND_ALREADY_EXISTS via `findFirst`). Full refunds only this slice; partial deferred. — 2026-05-23
- [x] `AdminPromos` — `admin-extras2.jsx::AdminPromos` (`/promos`) — KPI strip (Active/Scheduled/Expired/Inactive counts) + 5 filter tabs + create/edit `PromoFormDialog` (code/bilingual desc/type/value/max_uses/dates) + inline Edit/Delete. Backend: new `Promo` model + `PromoType` enum + 4 CRUD endpoints (list with derived status + counts, create with P2002→409, partial update, soft-delete). `derivePromoStatus` pure helper (active/scheduled/expired/inactive based on is_active + dates vs now). Admin-only management; customer cart redemption is a follow-up. — 2026-05-24
- [x] `AdminBanners` — `admin-extras2.jsx::AdminBanners` (`/banners`) — filter tabs (All/Live/Draft) + flex-column card list with ↑↓ arrow-button reorder, 120×60 image thumbnail, bilingual title (AR+EN), CTA URL, click_count, status pill, Edit/Delete inline. `BannerFormDialog` for create + edit with image upload via new `StorageService.uploadBannerImage` (same sharp+WebP+R2 pipeline as products, `banners/` key prefix). Backend: new `Banner` model + `BannerStatus` enum + 6 endpoints including `POST /admin/banners/:id/move { direction: up|down }` (atomic position swap in `$transaction`). No dnd-kit dep — arrow buttons sufficient for ≤20 banners. — 2026-05-24
- [x] `AdminStaff` (user roles + audit log viewer) — `admin-extras2.jsx::AdminStaff` (`/staff`) — read-only split (1fr / 1.1fr): left = active ADMIN-role users with avatar initials + online dot (5-min window) + role pill + last-login; right = audit feed from existing `AuditLog` model with color-coded accent bars by action (success/danger/info/amber). Invite button rendered disabled with `title=inviteComingSoon`. — 2026-05-22
- [x] `AdminTemplates` (SMS / push templates **viewer**) — `invoice-and-templates.jsx::AdminTemplates` (`/templates`) — Read-only viewer of the 10 actually-wired SMS templates (8 order events from `orders/helpers/sms-templates.ts` + ORDER_REFUNDED + CART_ABANDONED added this slice). 280px left list grouped by category (order/payment/shipping/marketing) + right detail pane with channel toggle (SMS active; Email/WhatsApp disabled with "Coming in 3b-3" tooltip), AR + EN bodies side-by-side, `{{variable}}` placeholders highlighted in amber chips, extracted variable list at top. Backend: new `GET /admin/templates` endpoint renders templates via `pickOrderSms(EVENT, lang, placeholderCtx)` with `total: '{{total}}' as unknown as number` cast trick to preserve template signatures. Editable templates (DB-backed) explicitly deferred — TS constants stay single source of truth. — 2026-05-23
- [x] `AdminShippingLabels` — `final-additions.jsx::AdminShippingLabels` (`/shipping-labels`) — 460px left scroll list (checkbox-selectable; zone + COD pills; Ready/Printed status) with status-filter tabs + flex-1 right preview pane with browser `window.print()` button (Download PDF rendered disabled with "Use browser print → Save as PDF" tooltip). Labels are pure-CSS 4×6" cards mirroring `final-additions.jsx::ShippingLabelPreview` (logo + ship-from + ship-to + zone pill + optional COD band + fake-barcode SVG). Print CSS in dedicated `print-styles.css` (only `.ship-print-area` visible, each `.ship-label` is its own 4×6 page via `page-break-after`). Backend: new `tracking_number`/`label_printed_at` columns on Order, `GET /admin/shipping-labels?status` + `POST /admin/shipping-labels/mark-printed` (max 100 ids per batch). Tracking number is derived from order_number (`BRT-…` → `BTL-…`) — no carrier API in scope. Print mutation fires in parallel with `window.print()`. — 2026-05-23
- [x] `AdminInventoryLog` — `final-additions.jsx::AdminInventoryLog` (`/inventory-log`) — 4 KPI cards (today_count / net_change / low_stock / pending_pos placeholder) + type-filter pills + grouped-by-day table with type badges. Backend: new `InventoryMovement` model + `InventoryMovementType` enum + 4 auto-log hook points (SALE on order create, RETURN on customer+admin cancel, RESTOCK/ADJUST classified by stock-delta sign in `admin.updateProduct`). New `GET /admin/inventory/movements?type&product_id&from&to&page&limit`. All hooks wrapped in defensive try/catch — failures logged, never roll back outer transaction. — 2026-05-23
- [x] `AdminAbandonedCarts` — `final-additions.jsx::AdminAbandonedCarts` (`/abandoned-carts`) — Summary KPI strip (active_carts / recoverable_value / items_in_carts) + stage-filter pills (cart/address/payment) + table with avatar/name/phone/items/value/age/stage/recovery columns; WhatsApp button = `wa.me` deep-link with pre-filled bilingual recovery message; SMS button disabled (`title="Coming in 3b-2"`). Backend: cart.service now mirrors Redis writes to the previously-unused `CartSession` DB model via fire-and-forget upsert (both sync + async failures caught + logged). New `GET /admin/abandoned-carts?stage&min_age_hours&page&limit` reads `cart_sessions` joined to User, computes stage from default-address presence, computes recovery score (`hot` if qty≥3 or value>200k SDG, `warm` if items≥1 and <6h old, else `cold`). Funnel chart deferred (needs page-view tracking we don't have). — 2026-05-23

### Operations
- [ ] Bulk product import (CSV → preview → confirm → transactional batch)
- [ ] Export CSV for orders / customers / inventory

## Phase 7 — Infrastructure (`infra/`)

### Containers
- [ ] `infra/docker/api.Dockerfile` (multi-stage, prod node, prisma generate at build)
- [ ] `infra/docker/web.Dockerfile` (Next.js standalone output)
- [ ] `infra/docker/admin.Dockerfile` (Vite static build → nginx serve)
- [ ] `infra/docker-compose.prod.yml` (api + web + admin + postgres + redis + nginx + certbot)

### Edge + DNS
- [ ] Nginx reverse proxy: `bartal.sd` → web, `admin.bartal.sd` → admin, `api.bartal.sd` → api
- [ ] SSL via Let's Encrypt (certbot renewal cron)
- [ ] Cloudflare DNS records + R2 bucket (public `bartal-products`, private `bartal-receipts`)

### Provisioning
- [ ] Hetzner / DO provisioning script (Terraform or bash) — region close to Khartoum (Frankfurt likely)
- [ ] DB backup cron (daily `pg_dump` → R2)
- [ ] Postgres tuning for low-RAM instance

### CI/CD
- [ ] GitHub Actions: lint + typecheck + test on every PR (matrix per workspace)
- [ ] GitHub Actions: build images + push to registry on `main` tag
- [ ] GitHub Actions: SSH deploy to prod with health-check rollback

### Observability
- [ ] Sentry DSN per app (api / web / admin)
- [ ] Structured logs → file → log shipper (loki or papertrail)
- [ ] Uptime ping (`/health`) every 1 min

---

## Activity Log

- **2026-05-24T13:24:06Z** — **Phase 6 Slice 3b-3 — catalog growth & settings shipped. Phase 6 COMPLETE (24/24).** 2 new admin routes (`/promos`, `/banners`) + 1 nested route (`/settings/legal/:slug`) + 6 settings tabs flipped from "Coming soon" to live forms. 2 new Prisma models (`Promo` + `Banner`) + 3 new enums (`PromoType`, `BannerStatus`; `BannerStatus` + `PromoType`). Migration `add_promos_and_banners`. ~43 new `AppSetting` keys seeded (Checkout: 10, Tax: 6, Locale: 2, Legal: 18, Integrations: 7). **Backend:** ~12 new admin endpoints (4 Promo CRUD + 6 Banner CRUD/move/image + banner image upload via new `StorageService.uploadBannerImage`). `derivePromoStatus` pure helper in `promo-status.ts`. Audit extended: `PROMO`/`BANNER`/`LEGAL_PAGE` entities + `DELETE`/`REORDER` actions. **Frontend:** `PromosPage` + `PromoFormDialog` (create/edit modal, code uppercased, type/value/dates, P2002→409 toast). `BannersPage` + `BannerFormDialog` (image upload + bilingual titles + status + CTA; ↑↓ arrow-button reorder via `POST /admin/banners/:id/move`). 6 new settings tab components (`CheckoutTab` with payment-methods reorder list, `TaxTab` with "config-only" disclaimer, `LocalesTab` read-only AR/EN cards, `TeamTab` link-to-/staff alias, `LegalTab` 6-page table + `LegalEditorPage` at `/settings/legal/:slug` with bilingual textarea + status, `IntegrationsTab` 7 toggles with disabled Configure buttons). `SettingsPage.tsx` tabs all flipped `active: true`; catch-all "Coming soon" fallback removed. Sidebar gained Promos + Banners; `COMING_SOON` array now empty (`marketing` removed — it never had a target page; promos+banners cover the marketing cluster). **Tests:** 237 total passing (was 220; +17 new: 6 promo-status pure helper + 3 listPromos/createPromo/deletePromo + 4 createBanner/moveBanner/BANNER_AT_EDGE/uploadBannerImage + 1 promos P2002). `pnpm --filter @bartal/admin build` clean — main chunk 658 kB. **Phase 6 is done.** All 24 admin pages shipped.
- **2026-05-23T18:13:45Z** — **Phase 6 Slice 3b-2 — order ops & compliance shipped.** 3 new admin routes (`/refunds`, `/shipping-labels`, `/templates`) + 7 new backend endpoints + 1 new Prisma model + 3 new Order columns + 2 new SMS templates + the 3b-1 carry-over (enable AbandonedCarts SMS button + dedicated send endpoint + `CART_ABANDONED` template). **Schema:** new `RefundStatus` enum (PENDING/APPROVED/REJECTED), new `RefundRequest` model (`refund_number` unique `RFD-YYYY-NNNNN`, `order_id` FK, `amount` Decimal(12,2), `reason` Text, `status` PENDING default, `rejection_reason` nullable VarChar(255), `requested_by_id` + `decided_by_id` FK to User with named relations `RefundRequester`/`RefundDecider`, `decided_at` nullable; 2 indexes on `(status, created_at)` and `(order_id)`). Order gained `tracking_number?`, `label_printed_at?`, `refunded_at?`. Migration `20260523180024_add_refunds_and_tracking_number`. **State machine extended:** `PAYMENT_CONFIRMED|PROCESSING|SHIPPED|CANCELLED → REFUNDED` added to `orders/helpers/state-machine.ts` (existing `DELIVERED → REFUNDED` retained); new `refundEligibleStatuses` set exported for the eligibility check at refund-create time. **SMS templates:** `ORDER_REFUNDED` (AR + EN, references order_number + total) and `CART_ABANDONED` (AR + EN, references customerName which is a new optional field on `OrderSmsContext`). `OrderSmsContext.customerName?: string` added; existing callers unaffected. **Refund number helper:** new `apps/api/src/modules/admin/helpers/refund-number.ts` mirroring `nextOrderNumber` (count-based per year, P2002 retry once at insert site). **Audit:** 3 new entity types (`REFUND`, `SHIPPING_LABELS`, `ABANDONED_CART_SMS`) + 4 new actions (`REFUND_APPROVED`, `REFUND_REJECTED`, `LABELS_PRINTED`, `ABANDONED_CART_SMS_SENT`). **Backend endpoints (7):** `GET /admin/refunds?status&page&limit` (with `$transaction([list, total, pending, approved, rejected])` counts payload), `POST /admin/refunds` (admin-initiated, validates eligibility + P2002-retry on `RFD-…`), `POST /admin/refunds/:id/approve` (atomic `$transaction`: refund APPROVED + Order REFUNDED + history + audit + post-commit SMS), `POST /admin/refunds/:id/reject` (refund REJECTED + reason + audit; no order side-effect), `GET /admin/shipping-labels?status` (ready/printed/all; joins address + items + customer + COD flag), `POST /admin/shipping-labels/mark-printed` (max 100 ids per batch via `ArrayMaxSize`; per-order: derive `BTL-…` tracking from `BRT-…` if not already set + advance PAYMENT_CONFIRMED→PROCESSING via state-machine; single audit summary row), `GET /admin/templates` (renders all 10 templates via `pickOrderSms(EVENT, lang, placeholderCtx)` with `total: '{{total}}' as unknown as number` cast to keep template signatures intact). **Plus 1 carry-over:** `POST /admin/abandoned-carts/:userId/sms` (24h rate-limit by `audit_log` lookup — cheap query, no new column; cleanly returns 409 RATE_LIMITED on duplicate; uses `NotificationsService.sendSms` injected into AdminService — global module, no module import needed). **NotificationsService** now injected into AdminService constructor (4 deps total). **Frontend (admin):** `apps/admin/src/routes/refunds/RefundsPage.tsx` (~290 lines — header with `+ New refund` button, KPI strip 3 cards from counts payload, 4 filter tabs, urgent-row tinting when PENDING <24h, inline approve/deny actions, inline reject-reason prompt without modal); `apps/admin/src/routes/refunds/RefundCreateDialog.tsx` (~110 lines — order_id text input, amount number input, reason textarea, surfaces API error envelopes via `apiErr.message_<locale>` toast); `apps/admin/src/routes/shipping-labels/ShippingLabelsPage.tsx` (~190 lines — dual-pane 460px list + flex-1 preview, status-filter tabs, multi-select via local `useState<string[]>`, parallel `mark-printed` mutation + `window.print()` via `queueMicrotask`); `apps/admin/src/routes/shipping-labels/ShippingLabelPreview.tsx` (~70 lines — pure-CSS 4×6 label matching `final-additions.jsx::ShippingLabelPreview` with logo + ship-from + ship-to + zone pill + optional amber COD band + fake-barcode SVG); `apps/admin/src/routes/shipping-labels/print-styles.css` (`@page { size: 4in 6in; margin: 0 }` + `body * { visibility: hidden }` reveal pattern, `.ship-label { page-break-after: always }`); `apps/admin/src/routes/templates/TemplatesPage.tsx` (~170 lines — 280px left list grouped by category, right detail pane with channel toggle, AR+EN side-by-side bodies with `highlightPlaceholders` helper rendering `{{variable}}` chunks in amber-tint chips). **AbandonedCarts SMS button** flipped from disabled + tooltip to live button calling `useSendAbandonedCartSms` mutation; success toast in user's locale; 409 RATE_LIMITED shows info toast "Already sent today" rather than error. **API layer:** types extended with `RefundStatusValue`, `RefundFilter`, `AdminRefundRow`, `AdminRefundListResponse`, `ShippingLabelFilter`, `ShippingLabelRow`, `ShippingLabelsResponse`, `TemplateEvent`, `AdminTemplateRow`, `AdminTemplatesResponse`. `queries.ts` gained `useAdminRefunds`, `useAdminShippingLabels`, `useAdminTemplates`. `mutations.ts` gained `useCreateRefund`, `useApproveRefund`, `useRejectRefund`, `useMarkLabelsPrinted`, `useSendAbandonedCartSms`. **Dictionary:** 3 new nav keys per locale (`refunds`, `shippingLabels`, `templates`) — page copy inlined via `locale === 'ar' ? '…' : '…'` ternaries as in 3b-1. **Tests:** +13 new unit tests covering listRefunds counts payload + filter, createRefund eligibility 409 paths + happy + audit + collision retry, approveRefund tx orchestration + SMS fire + non-PENDING 409, rejectRefund no-order-side-effect, markLabelsPrinted CONFIRMED→PROCESSING + leaves PROCESSING alone + 404 on unknown id, getTemplates 10 rows with variables, sendAbandonedCartSms happy + 24h rate-limit 409. Updated admin spec mock to add `refundRequest` + `orderStatusHistory` + `order.update` + injected NotificationsService stub. **Verified:** `pnpm --filter @bartal/api test` **220 unit / 13 suites passing** (was 207 in 3b-1, +13 new). `pnpm --filter @bartal/api build` clean. `pnpm --filter @bartal/admin build` clean — main chunk 617 kB (up from 593 — +24 kB for the 3 new pages + RefundCreateDialog + ShippingLabelPreview + new types/queries/mutations). Recharts/RevenueChart/OrdersByStatusChart/AnalyticsSalesChart still lazy-loaded. **Trade-offs documented:** (1) **Refund creation is admin-initiated only** — no web changes this slice; customer self-service deferred. AdminRefunds page gets `+ New refund` button accepting `order_id + amount + reason`. (2) **Full refunds only** — `RefundRequest.amount` defaults to full Order.total at request time; partial-refund column is a follow-up. (3) **Tracking number is synthetic** (`BRT-…` → `BTL-…`) — no Sudan carrier API in scope; the field exists to surface the print timestamp admin-side. (4) **Browser print, no PDF library** — same pattern as web's `WebInvoicePrint`. Download PDF button disabled with "Use browser print → Save as PDF" tooltip. (5) **Templates viewer is read-only of the 10 wired templates** — review_request, low_stock, promo_blast etc. from the design's 12-template list are NOT shown (only the 8 OrderSmsEvent + 2 new templates this slice). DB-backed editing deferred. (6) **AR + EN only on Templates** — Email + WhatsApp channels rendered disabled with "Coming in 3b-3" tooltip; the backend has no email sender and PRD describes WhatsApp as human-driven not API-driven. (7) **24h SMS rate-limit via audit_log lookup** — `auditLog.findFirst({ entity_type: ABANDONED_CART_SMS, entity_id: userId, action: ABANDONED_CART_SMS_SENT, created_at: { gte: cutoff } })`. Cheap and reuses existing infra; no new column. Returns ConflictException with code `RATE_LIMITED` so the frontend can distinguish from a real error. (8) **Refund creation has no order autocomplete** — admin pastes `order_id` (cuid). Cleaner UX defers to later. (9) **mark-printed batch rejects on first invalid id** — UX is "all or nothing" per click; pre-validate selection client-side if you want softer behavior. (10) **Approving a refund DOES advance Order status to REFUNDED in one shot** (atomic tx). No "approved but pending payout" intermediate state — Sudan ops will issue the bank refund manually and the order is considered closed once the admin clicks Approve. **What this unblocks:** ops can now (a) approve a customer's refund request that came in over WhatsApp and see the SMS land, (b) batch-select 30 ready-to-ship orders and print 4×6 thermal labels from a browser, (c) audit which SMS templates are actually wired before tweaking copy in code, (d) nudge abandoned carts with a one-tap SMS (rate-limited). Phase 6 admin is now 21/24; only the catalog-growth-settings cluster (Promos + Banners + Settings tabs 3–8) remains for Slice 3b-3.
- **2026-05-23T17:13:46Z** — **Phase 6 Slice 3b-1 — analytics + read-only reporting shipped.** 3 new admin routes (`/analytics`, `/inventory-log`, `/abandoned-carts`) + 2 new backend endpoints + 1 extended endpoint + 1 new Prisma model + auto-log hooks on order events + cart-sessions DB mirror. **Schema:** new `InventoryMovementType` enum (SALE/RETURN/RESTOCK/ADJUST), new `InventoryMovement` model (`product_id`, `sku?`, `type`, `quantity` signed, `stock_after`, `reference?`, `actor_id?` nullable FK to users, `created_at`; 2 indexes on `(product_id, created_at)` and `(type, created_at)`), back-refs added on `Product.inventory_movements` and `User.inventory_movements`. Migration `20260523165314_add_inventory_movements`. **Backend hook points:** `orders.service.ts` writes SALE rows inside the order-create `$transaction` (one per line, `quantity = -r.quantity`, `actor_id = null`); RETURN rows on customer + admin cancel (`quantity = +item.quantity`, `actor_id = userId/adminId`); `admin.service.ts::updateProduct` writes RESTOCK row if `dto.stock > before.stock` else ADJUST if `<`. All four sites wrapped in `try/catch` → `logger.warn` on failure; the audit insert never rolls back the outer transaction (same defensive pattern as `writeAudit`). `cart.service.ts::writeItems` now mirrors `redis.setex` to `prisma.cartSession.upsert` fire-and-forget (both sync + async failures caught); `clear` mirrors `redis.del` to `prisma.cartSession.deleteMany`. **Endpoints:** `GET /admin/inventory/movements?type&product_id&from&to&page&limit` returns `{ items, total, page, limit, kpis: { today_count, net_change_today, low_stock_count, pending_pos } }` — `low_stock_count` uses raw SQL `SELECT COUNT(*) FROM products WHERE is_active AND stock <= low_stock_threshold` (Prisma's typed `where` can't do column-to-column comparison). `GET /admin/abandoned-carts?stage&min_age_hours&page&limit` reads `cart_sessions` joined to User (filters out users with any orders; only `updated_at <= now - min_age_hours`), joins to Product for cart_value via in-memory aggregation, computes stage (`address` if user has default address, else `cart`), computes recovery score (`hot` if items≥3 or value>200k SDG, `warm` if items≥1 and <6h old, else `cold`), returns `{ items, total, page, limit, summary: { active_carts, recoverable_value_sdg, items_in_carts } }`. Extended `GET /admin/analytics/sales` with optional `breakdown=zone` query param backed by new private `dailyRevenueByZone` raw SQL helper (`date_trunc('day', o.created_at), addresses.zone` group-by). New DTOs: `InventoryMovementsQueryDto`, `AbandonedCartsQueryDto`, `SALES_BREAKDOWNS` constant. **Frontend:** `apps/admin/src/routes/analytics/AnalyticsPage.tsx` (range selector 7/30/90 + breakdown=zone toggle + KPI strip Revenue/Orders/AOV + lazy-loaded `AnalyticsSalesChart.tsx` with stacked area when breakdown=zone + top-10 sellers list); `apps/admin/src/routes/inventory-log/InventoryLogPage.tsx` (4 KPI cards + 5 type-filter pills (All/Sale/Restock/Return/Adjust) + grouped-by-day table with type badges color-coded by `TYPE_STYLE` map + URL-driven pagination); `apps/admin/src/routes/abandoned-carts/AbandonedCartsPage.tsx` (3 summary KPIs + 4 stage-filter pills + table with avatar/name/phone/items/value/age/stage/recovery columns + WhatsApp anchor `wa.me/${phone}?text=${bilingual greeting}` opens in new tab + SMS button rendered disabled with `title="Coming in 3b-2"`). **Sidebar reordered** to insert the 3 new entries in the agreed slot: Dashboard → Orders → Reviews → Products → Categories → Customers → **Inventory log → Abandoned carts → Analytics** → Zones → Staff → Settings. Only `marketing` remains in COMING_SOON. **API layer:** `apps/admin/src/lib/api/types.ts` got `SalesBreakdown`, `SalesAnalyticsDay`, `SalesAnalyticsDayByZone`, `SalesAnalyticsResponse`, `AdminAnalyticsTopProduct`, `AdminAnalyticsTopProductsResponse`, `InventoryMovementType`, `InventoryMovementRow`, `InventoryMovementsResponse`, `AbandonedCartStage`, `AbandonedRecoveryScore`, `AbandonedCartRow`, `AbandonedCartsResponse`. `queries.ts` gained `useAdminAnalyticsSales(params)`, `useAdminTopProducts(limit)`, `useAdminInventoryMovements(params)`, `useAdminAbandonedCarts(params)`. **Dictionary:** 3 new nav keys per locale (`analytics`, `inventoryLog`, `abandonedCarts`) — all other UI copy is inlined in each page via `locale === 'ar' ? '…' : '…'` ternaries since this is a single-slice greenfield and adding 30+ nested keys would bloat the dictionary; bilingual coverage is complete on every label. **Tests:** +9 new unit tests (3 in cart.service.spec for the DB mirror + 6 in admin.service.spec — 3 for getInventoryMovements (filter + clamp + invalid-date), 2 for getAbandonedCarts (enriched response + stage filter), 1 for salesAnalytics breakdown=zone). Updated 4 existing orders.service.spec tests to assert the new `select: { id, sku, stock }` shape on `prisma.product.update` calls + added new positive assertions that `inventoryMovement.create` was called with the right `{ type, quantity, actor_id }` per code path. Cart spec mock gained `cartSession: { upsert, deleteMany }`. **Verified:** `pnpm --filter @bartal/api test` → **207 unit / 13 suites passing** (was 198, +9 new). `pnpm --filter @bartal/api build` clean. `pnpm --filter @bartal/admin build` clean — main chunk 593 kB (up from 577 — +16 kB for the 3 new pages + chart + new types/queries). Recharts/RevenueChart/OrdersByStatusChart/AnalyticsSalesChart all still lazy-loaded. **Trade-offs documented:** (1) **Funnel chart deferred** — design's Browse → Cart → Address → Payment → Review percentages need page-view tracking we don't have; v1 ships the carts table + summary KPIs only. Revisit after Phase 7 analytics infra. (2) **"Pending POs" KPI is a placeholder** (always 0) — no PO model exists yet; UI shows "Coming soon" hint. (3) **"Conversion rate" KPI dropped from Analytics page** — same tracking gap; UI shows Revenue/Orders/AOV instead. (4) **Real SMS send from Abandoned Carts disabled** — button rendered with `disabled` + tooltip "Coming in 3b-2"; WhatsApp `wa.me` deep-link works today. (5) **`cart_sessions` is a fire-and-forget mirror, not the source of truth** — Redis still authoritative for live reads (TTL + low latency); DB mirror exists only for admin queryability. Sync failures don't roll back cart writes; eventual consistency is acceptable. (6) **Inventory log inside-tx writes catch errors and continue** — if the audit insert fails, the order/product transaction still commits with stock changes applied; the missing row shows in `logger.warn` only. Same trade-off as `audit_logs`. (7) **Abandoned-carts stage heuristic is coarse** — `cart` if no default address, `address` if default address exists, `payment` only inferred from active draft orders (not yet wired this slice — always `address` or `cart` in v1). (8) **AdminAnalytics doesn't yet show comparison vs previous period** — design has an "↑12% vs last week" line that needs a second range query; deferred to keep the slice tight. (9) **No CSV export** despite the design's "Export CSV" button on InventoryLog — deferred alongside other exports listed under Phase 6 → Operations. (10) **Page-level i18n copy inlined via ternaries** instead of extending the dictionary by 30 keys — fine for one slice; if 3b-2/3b-3 reuse identical labels, promote to dictionary then. **What this unblocks:** ops can see today's stock movements grouped by day; analytics is one click rather than scrolling the dashboard; abandoned carts gives the support team an actionable recovery list with one-tap WhatsApp. Slice 3b-2 (Refunds/ShippingLabels/Templates) can now reuse the `cart_sessions` mirror, the `InventoryMovement` audit, and the page-pattern (KPI strip + tabbed table + URL params) established here.
- **2026-05-22T12:02:47Z** — **Phase 6 Slice 3a — moderation + staff/audit shipped.** Two new admin routes (`/reviews`, `/staff`) + 7 new backend endpoints + Review schema gained moderation columns. **Schema:** new `ReviewModerationStatus` enum (PENDING/APPROVED/REJECTED), 5 new columns on `reviews` (`moderation_status` default PENDING, `flagged_reason`, `rejection_reason`, `moderated_by` FK to users, `moderated_at`), 2 new indexes (`(moderation_status, created_at)`, `(moderation_status, flagged_reason)`), reverse relation `User.moderated_reviews`. Migration `20260522113519_add_review_moderation` backfills existing rows to APPROVED so the customer storefront stays uninterrupted. **Backend endpoints:** `GET /admin/reviews?status=pending|flagged|approved|rejected|all&page&limit&search` (Pending filters PENDING+flagged_reason null; Flagged filters PENDING+flagged_reason not null), `GET /admin/reviews/kpis` (pending/flagged/approvedThisMonth counts + avg rating 30d + verified buyer % + avg response hours via raw SQL `EXTRACT(EPOCH FROM AVG(moderated_at-created_at))`), `POST /admin/reviews/:id/approve`, `POST /admin/reviews/:id/reject` (body: `{ reason: string }` min-3 chars), `POST /admin/reviews/:id/reset`, `GET /admin/staff` (role=ADMIN+is_active=true, sorted last_login desc nulls last), `GET /admin/audit-log?entity_type&actor_id&limit` (max 100, default 50). All review mutations write `AuditLog` rows via the existing `writeAudit` helper with new actions `REVIEW_APPROVED`/`REVIEW_REJECTED`/`REVIEW_RESET_TO_PENDING` and entity `Review`. **Auto-flag:** `apps/api/src/modules/reviews/helpers/auto-flag.ts` — pure function scans review comment for URL regex + 5 spam keywords (`defective, broken, fake, scam, refund`); returns compound reason "spam keywords + URL" if both present, otherwise the matched single trigger. Customer `POST /products/:id/reviews` now always inserts as PENDING + writes `flagged_reason` if matched. **Customer-side filter:** `ReviewsService.list()` `count` + `findMany` + `groupBy` queries all now filter `moderation_status: 'APPROVED'` so rejected/pending reviews never reach customers; existing `reviews.e2e-spec.ts` updated to promote review to APPROVED before asserting the customer list, plus a new assertion that customer list is empty before approval. **Frontend (admin):** `apps/admin/src/routes/reviews/ReviewsPage.tsx` (~290 lines, single file — KPI strip with 5 `AdmStatCard`s, 380px sidebar with `AdmTabs` + scroll list of rows with stars + flagged pill + verified pill + truncated comment, right-rail detail panel with auto-flag banner shown only when `flagged_reason && status=PENDING`, internal context grid showing product + customer, action zone toggling between approve-button-only-pair and reject-mode 7-chip picker, resolved banner for APPROVED/REJECTED states with "Move back to pending" button). `apps/admin/src/routes/staff/StaffPage.tsx` (~140 lines — read-only header with disabled "Invite member" button + `inviteComingSoon` tooltip, split grid: left team list with avatar initials + online dot via 5-min `last_login_at` window + role pill + relative last-login, right audit feed with color bar by action mapped through `ACTION_ACCENT`). Both pages use existing primitives (`AdmCard`, `AdmStatCard`, `AdmTabs`, `AdmEmptyState`, `AdmButton`, `pushToast` from `AdmToaster`) — no new primitives. **Router + sidebar:** `apps/admin/src/router.tsx` gained `reviews` + `staff` routes (lazy). `apps/admin/src/components/shell/Sidebar.tsx` `ACTIVE_ITEMS` now `dashboard, orders, reviews, customers, products, categories, zones, staff, settings`; `COMING_SOON` shrunk to `['marketing']`. **API layer:** `apps/admin/src/lib/api/types.ts` got `AdminReviewItem`/`AdminReviewListResponse`/`AdminReviewKpis`/`ReviewModerationStatus`/`AdminReviewFilter`/`AdminStaffMember`/`AdminStaffListResponse`/`AdminAuditEntry`/`AdminAuditListResponse`. `queries.ts` gained `useAdminReviews(params)`, `useAdminReviewKpis()`, `useAdminStaff()`, `useAdminAuditFeed(params)`. `mutations.ts` gained `useApproveReview()`, `useRejectReview()`, `useResetReviewToPending()` — all invalidate `['admin','reviews']` + `['admin','review-kpis']` + `['admin','audit-log']` and toast on success/error. **Dictionary:** ~90 new lines spanning `nav.reviews`, `reviews.*` (kpi labels + sub-labels, 4 tabs, list/empty/pill copy, auto-flag banner template with `{reason}` interpolation, internal context labels, action button copy, 7 reject reason chips, resolved banners, move-back), `staff.*` (subtitle with `{n}` interpolation, invite copy with coming-soon tooltip, team + audit headings, online + relative-time copy, role labels) — full AR translations included this slice (not deferred). **Tests:** +21 unit (16 admin.service.spec.ts for listReviews per filter + approve/reject/reset audit writes + 404, 5 reviews.service.spec.ts for APPROVED-only filter on count/groupBy and auto-flag at create) + 6 auto-flag.spec.ts (URL/keyword/combined). E2E `admin.e2e-spec.ts` extended: advances order to DELIVERED, customer posts clean review, prisma-seeds a flagged review for a synthetic second buyer, lists `?status=pending` (1) and `?status=flagged` (1), approves clean → public `GET /products/:id/reviews` includes it, rejects flagged with chip-string reason, validates 400 on `reason.length<3`, asserts review KPIs respond, staff list contains seeded admin, audit-log feed has ≥2 Review entries. `reviews.e2e-spec.ts` updated to handle PENDING-by-default. **Verified:** `pnpm --filter @bartal/api test` **198 unit / 13 suites passing** (was 177). `pnpm --filter @bartal/api test:e2e` **9/9 passing across 5 suites** (no new e2e file; extended admin spec covers Slice 3a in-line). `pnpm --filter @bartal/admin typecheck` clean (strict). `pnpm --filter @bartal/admin lint` clean (1 pre-existing react-refresh warning on AdmToaster). `pnpm --filter @bartal/admin build` clean — main chunk 576 kB (up from 560 — +16 kB for ReviewsPage + StaffPage + reason chip state + audit color map). Recharts/RevenueChart/OrdersByStatusChart still lazy-loaded. **Trade-offs documented:** (1) **Reply composer dropped** — no `Review.reply` column in schema; design's reply textarea is omitted. Re-add when reply infra (separate `ReviewReply` model or column + customer-visible thread) lands. (2) **"Invite member" rendered disabled** — needs email/SMS invite-token infra. Title attribute carries `inviteComingSoon` copy. (3) **Pending-count badge on sidebar skipped** — needs polling or websocket; KPI strip already shows pending count on the page. (4) **Median response → Avg response** — true median needs `PERCENTILE_CONT` raw query; ship `avg(moderated_at - created_at)` and rename label so UI isn't misleading. (5) **Granular User roles NOT introduced** — UserRole stays binary CUSTOMER|ADMIN; staff page lists where `role=ADMIN && is_active=true`. Granular tiers (MODERATOR/SUPPORT) defer to a future slice that needs the distinction. (6) **Backfill APPROVED** for existing reviews — customer storefront unchanged on day 1; only NEW reviews enter moderation. (7) **Auto-flag is hardcoded keywords** — `SPAM_KEYWORDS=['defective','broken','fake','scam','refund']` + URL regex. Moving to AppSetting is one PR away when ops wants to tune. (8) **Audit feed entity-id truncation** — UI shows `Review#a1b2c3` (last 6 chars of cuid) for compact display; full id in tooltip via underlying data if needed later. (9) **Online indicator uses 5-min window** — derived purely from `last_login_at`; no heartbeat. Acceptable for a low-traffic ops dashboard. (10) **Single-file ReviewsPage** instead of 7 sub-component files — page is ~290 lines, all components used only here, sub-component split was premature abstraction. **What this unblocks:** customer reviews are now actually moderated before customers see them; the storefront's review quality bar is enforceable; ops can see exactly who did what (and roll it back via Reset) through the same audit infra Slice 2 wrote into. Slice 3b candidates: Marketing CMS (Promos + Banners, both need new tables + R2 image upload + drag-reorder), Operations gaps (ShippingLabels + InventoryLog + AbandonedCarts), Templates + Analytics-deep, or 6 remaining Settings tabs.
- **2026-05-21T21:02:08Z** — **Phase 6 Slice 2 — admin catalog management shipped.** 4 new admin routes (`/products`, `/products/new`, `/products/:id/edit`, `/categories`) + 5 new backend endpoints (`GET /admin/products` with counts payload, `GET /admin/products/:id`, `GET /admin/categories`, `PUT /admin/products/:id/images/:imageId`, `DELETE /admin/products/:id/images/:imageId`). Admin module gained `IMAGE_UPDATE` + `IMAGE_DELETE` audit actions. **Backend:** `admin.service.ts` adds `getProducts` (status/category/q filters + `$transaction` of 5 counts driving the AdminProducts tab counts in one round trip; accepts category as slug or cuid; reshapes Decimal→number + ISO strings + primary image hoisted from `images: { where: { is_primary: true }, take: 1 }`), `getProduct` (full detail incl. images sorted by sort_order asc + category ref; no `is_active` filter so admins can load soft-deleted products), `getCategories` (flat with `_count.products`; tree assembled client-side), `updateProductImage` (transactional primary flip — `updateMany where id !== this set is_primary:false` then update target), `deleteProductImage` (hard-delete + auto-promote next image to primary when the deleted one was primary, ordered by `sort_order asc, id asc`). New DTO `UpdateProductImageDto` (is_primary/sort_order/alt_ar/alt_en, all optional). **Admin foundation:** types extended with `AdminProductListItem`, `AdminProductDetail`, `AdminProductImage`, `AdminCategoryNode`, `AdminProductListResponse`. Queries hooks `useAdminProducts(params)`, `useAdminProduct(id)`, `useAdminCategories()`. Mutations: `useCreateProduct`, `useUpdateProduct`, `useDeleteProduct`, `useUploadProductImage` (FormData multipart), `useUpdateProductImage`, `useDeleteProductImage`, `useCreateCategory`, `useUpdateCategory`. **Pages:** `ProductsPage.tsx` (5 status tabs reading the new counts payload + category slug dropdown + Tanstack Table + URL pagination + color-coded stock + status pill derived from is_active/stock/is_featured/out_of_stock). `ProductFormPage.tsx` (~370 lines, shared between create + edit; react-hook-form + zod resolver mirroring backend `CreateProductDto`; client-side slug auto-generate from name_en with manual-edit detection; soft-delete + activate via `is_active` checkbox; depth-indented category dropdown via client-side tree walk). `ImageManager.tsx` (file picker + Add Image button hits `POST /admin/products/:id/images` multipart; each image has a thumbnail + numeric sort_order input that PUTs on blur if changed + set-primary toggle + delete with native confirm; first image of an empty product is auto-marked primary on upload). `CategoriesPage.tsx` (left 2/3 hierarchical list rendered from flat data via `buildTree`, depth-indented with `paddingInlineStart`, expand/collapse via Set; right 1/3 edit panel with self+descendants excluded from parent dropdown to prevent cycles client-side before the backend's depth-5 cycle guard kicks in). **Shell + router:** Sidebar promotes `products` + `categories` out of COMING_SOON (only `marketing` + `staff` remain there). Router gains 4 routes nested under the existing `<RequireAuth><AdminShell />` layout. **Dictionary:** ~190 new lines spanning `products.*`, `productForm.*` (sections + fields + validation + media-manager strings), `categories.*` for both AR + EN. **Tests:** 8 new admin service unit tests covering `getProducts` filters + counts, `getProduct` happy + 404, `getCategories` shape, `updateProductImage` primary flip + IMAGE_NOT_FOUND, `deleteProductImage` auto-promote + no-promote-when-not-primary. Extended `admin.e2e-spec.ts` with catalog flow: lists products with counts; fetches detail; seeds 2 images directly via Prisma; PUT flips primary from img1→img2 (asserts img1's flag flipped to false); DELETE img2 (asserts img1 auto-promoted back to primary); 404 on unknown imageId; lists categories asserting product_count. **Verified:** `pnpm --filter @bartal/api test` **177 unit / 12 suites passing** (was 169). `pnpm --filter @bartal/admin typecheck` clean. `pnpm --filter @bartal/admin lint` clean (1 known react-refresh warning on AdmToaster). `pnpm --filter @bartal/admin build` clean — main chunk 560 KB (up from 528 — +32 KB for product form + image manager + categories tree); Recharts/RevenueChart/OrdersByStatusChart still lazy-loaded; CSS 21 KB. **Trade-offs documented:** (1) **Numeric `sort_order` instead of drag-to-reorder** — user-chosen, no dnd-kit dep this slice. (2) **Variants out of scope** — schema has variant models but no admin endpoints; ProductForm is single-SKU/single-price. (3) **No CSV import.** (4) **Image delete is hard-delete** — `ProductImage` has no soft-delete column; R2 object cleanup deferred to a Phase 2 sweeper so deleted images leak in R2 until then. (5) **Category icon picker deferred** — `image_url` exists on the schema; admin doesn't set it this slice. (6) **No category-delete endpoint** — categories soft-delete via `is_active=false` toggle in the same PUT; cascade preview deferred. (7) **Sold count column shows `—`** — no aggregate computed; future denormalised `units_sold` lands when ranking matters. (8) **Slug uniqueness enforced server-side** via Prisma `@unique` + existing P2002 → 409 catch; the form surfaces it via toast. (9) **Image upload happens AFTER product create** — form first saves the product → redirects to `/edit` → image grid becomes interactive. Avoids the "image uploaded but row missing" race; matches Shopify/Stripe pattern. (10) **Category form is non-modal** — right-rail panel inside `/categories` for fast batch editing. **What this unblocks:** the store can be **stocked from the admin UI** without any backend tool. Combined with Slice 1, the admin is end-to-end useful for daily Sudan ops — operators can add a product (bilingual + image upload + price + stock + category), publish it, edit it, soft-delete it, and manage the category tree all in-browser. Slice 3 candidates: marketing (promos/banners/reviews moderation — needs schema additions on `Review`), staff/audit log viewer, refunds, templates, abandoned carts. Or pivot to Phase 7 infra to actually deploy the stack and have ops users start exercising it.
- **2026-05-21T17:23:18Z** — **Phase 6 Slice 1 — admin dashboard operator-critical path shipped.** `apps/admin/` scaffolded from scratch (Vite + React 18 + TS strict + Tailwind + Tanstack Table + Recharts + react-router v6 + Zustand + react-hook-form/zod + react-zoom-pan-pinch). Workspace updated (`pnpm-workspace.yaml` adds `apps/admin`, approves `esbuild` build). **Routes shipped (9):** `/login` (split-panel auth, react-hook-form+zod, NOT_ADMIN guard); `/` (dashboard with 4 KPI cards + lazy-loaded Recharts area chart + bar chart + recent orders table + top sellers); `/orders` (status tabs + zone/date/q filters + Tanstack Table + URL-driven pagination); `/orders/:id` (status timeline + items + customer/shipping/notes sidebar + state-machine action bar with confirm/reject payment + start-processing/ship/deliver/cancel buttons; opens receipt viewer dialog with `react-zoom-pan-pinch` zoom + 4-row match-checker + approve/reject); `/customers` (paginated + debounced search); `/customers/:id` (hero + addresses + last 10 orders); `/zones` (inline-edit 4-row table; PUT /admin/delivery/zones/:zone per save); `/settings` (8-tab nav; General + Banking active, 6 tabs render "Coming soon"); `/login?error=NOT_ADMIN` for role-gated entry; `*` 404 page. **Foundation:** `apps/admin/src/lib/api/client.ts` (axios + 401-refresh-once interceptor — sequential calls share one in-flight refresh promise; calls `setTokens` on success), `lib/auth/store.ts` (Zustand WITHOUT `persist` — admin tokens in memory only per CLAUDE.md §4; on hard reload the operator re-logs in), `lib/auth/api.ts`, `lib/auth/useAuth.ts` (login asserts `user.role === 'ADMIN'`), `lib/auth/RequireAuth.tsx` (route guard with `next=` query for return path), `lib/api/queries.ts` (8 React Query hooks), `lib/api/mutations.ts` (4 mutations with query invalidation). **Primitives (from-scratch Tailwind, matching web; no Radix this slice):** `AdmButton`, `AdmCard`, `AdmInput`, `AdmTextarea`, `AdmSelect`, `AdmStatCard`, `AdmStatusPill`, `AdmEmptyState`, `AdmDialog` (focus trap + Escape close + restore-focus-on-close + body scroll lock + click-outside), `AdmToaster` (singleton subscribe pattern; `pushToast(kind, message)` from any component), `AdmTabs`, `BartalLogo` (8-point star + amber center), `PriceTag`, `AdmStatusTimeline`. **Shell:** 220px navy `Sidebar` (8 nav items: 5 active + 4 "Soon" pills for products/categories/marketing/staff) + `Topbar` (title from a Zustand topbar-store set per-page via `useTopbarTitle` hook, locale toggle, theme toggle persisting to localStorage, avatar + logout). **Backend (+1 endpoint):** `GET /admin/orders/:id` returns enriched `AdminOrderDetail` (items with image_url, user, address, history reverse-ordered) — required because the customer-facing `GET /orders/:id` filters by `user_id` so admins can't read other users' orders. +2 unit tests (happy path + 404). Extended `admin.e2e-spec.ts` to assert detail after the existing status-progression flow + a 404 on unknown id. **Token promotion:** `packages/shared/src/design/tokens.ts` created (canonical `BARTAL` + `fmtSDG` + `PLACEHOLDER_PALETTES`); `apps/web/src/design/tokens.ts` refactored to re-export from there. **`apps/admin/src/design/tokens.ts` inlines the values directly** — Vite's Rollup can't tree-shake CJS re-exports through `__exportStar`, so the admin keeps its own copy (~30 lines; explicit duplication acceptable per CLAUDE.md §5 "each surface re-translates the same tokens"). **i18n:** `apps/admin/src/lib/i18n/dictionary.ts` with `AdminDictionary` interface and `en`/`ar` records — covers nav, login, dashboard, orders + tabs, order detail actions, receipt viewer, customers, zones, settings tabs, common (loading/error/none). Default locale `en` (admins are mostly bilingual but operators in the design use EN as default chrome language). Toggle persists to localStorage. **Verified:** `pnpm --filter @bartal/admin typecheck` clean (strict). `pnpm --filter @bartal/admin lint` clean (1 minor `react-refresh` warning on `pushToast` colocated with `AdmToaster` — acceptable). `pnpm --filter @bartal/admin build` produces `dist/` (528 kB main chunk + 372 kB lazy Recharts chunk + 11 kB lazy RevenueChart + 1 kB OrdersByStatusChart + 20 kB CSS — login bundle never loads charts). `pnpm --filter @bartal/web build` still clean (40 routes; `.next` wipe required to clear stale chunks). `pnpm --filter @bartal/api test` **169 unit / 12 suites passing** (was 167 — added 2 for `getOrder`). **Trade-offs documented:** (1) In-memory tokens force re-login on hard reload — CLAUDE.md §4 strict reading; the 401-interceptor handles in-tab token expiry transparently. (2) No command palette / `⌘K` — sidebar nav suffices for 9 routes. (3) Receipt match-checker is local state only — not persisted. (4) Customer profile read-only (no editing endpoint in scope). (5) Settings ships 2 of 8 tabs; the other 6 stub as "Coming soon". (6) No admin Playwright e2e this slice — backend e2e contracts the API; admin UI relies on manual smoke until Phase 7 infra ships CI. (7) Duplicate primitives between web and admin (`BartalLogo`, `PriceTag` etc.) — CLAUDE.md §5 accepts this; extraction to `@bartal/ui` deferred. **What this unblocks:** the daily Sudan ops workflow is now end-to-end usable — customer registers via web → places bank-transfer order → uploads receipt → admin logs in to `/orders?tab=RECEIPT_UPLOADED` → opens detail → views zoomable receipt → toggles 4 match-checker fields → approves payment → starts processing → marks shipped → marks delivered → customer sees the AR SMS land. Phase 6 Slice 2 can focus on catalog management (products + categories + image upload) reusing every primitive shipped here.
- **2026-05-21T16:28:11Z** — **Web Phase 5 closed.** Final slice: auth glue + SEO/perf polish, bundled per user decision. **Auth glue** — new `apps/web/src/lib/auth/cookies.ts` (server-only): writes `bartal_access` (15m, httpOnly+Secure+SameSite=Lax), `bartal_refresh` (30d, same flags), and `bartal_user` (30d, **non-httpOnly** JSON so the client `useAuth()` hook can read user identity without a server roundtrip — tokens stay locked down). New `apps/web/src/lib/auth/actions.ts` exports 7 Server Actions (`loginAction`, `registerAction`, `verifyOtpAction`, `resendOtpAction`, `forgotPasswordAction`, `resetPasswordAction`, `logoutAction`) — each returns a discriminated union `{ ok, data } | { ok, error: { code, message_en, message_ar, status } }`, never throws across the action boundary; `verifyOtpAction` writes cookies only for REGISTER/LOGIN purposes (RESET keeps the user unauthenticated until they finish the new-password flow). New `apps/web/src/lib/auth/state.ts` exports a client `useAuth()` hook (reads `bartal_user`, cross-tab sync via `BroadcastChannel('bartal-auth')`, `logout()` method). New `apps/web/src/lib/api/auth.ts` — eight thin `apiPost` wrappers mirroring the backend's 8 endpoints. Extended `apiClient` with `apiPost`/`apiPut`/`apiDelete` (axios wrappers that unwrap the bilingual `{ success, data, error }` envelope and throw `ApiClientError`). **Middleware extended** at `apps/web/src/middleware.ts` — protected matcher `^/(ar|en)/(account|orders|wishlist|checkout)`; if access cookie missing but refresh present, calls `POST /auth/refresh` server-side and rewrites cookies; both missing → 302 to `/<locale>/login?next=<encoded>`. **Cart sync** — new `apps/web/src/lib/cart/sync.ts` Server Action `syncCartAction({items, locale})`: reads access cookie, POSTs each local line to `cart/items`, then `GET /cart` and returns server view + per-line conflicts. `cart-store` gained `hydrateFromServer(items)` + `setSyncing(value)` actions. **Pages** — login/register/forgot-password refactored from "Coming soon" stubs to thin server components that render new client form components (`LoginForm`/`RegisterForm`/`ForgotPasswordForm`) wrapping a refactored `AuthCard` (now accepts `onSubmit` + error state, dropped the showNotice stub). **Two new pages added (40/40 web routes total):** `/<locale>/verify-otp` (`OtpForm`: 6 digit boxes with paste auto-distribute + auto-submit on full code + 60s resend countdown + REGISTER/LOGIN/RESET purpose dispatch — RESET hop sends user to `/reset-password?phone=…&code=…`) and `/<locale>/reset-password` (`ResetPasswordForm`: takes phone+code from search params, new password + confirm with mismatch check, redirects to `/login` on success). LoginForm + OtpForm trigger `syncCartAction` on success and surface conflict banner ("N items skipped — out of stock"). **SEO** — new `apps/web/src/lib/seo/site.ts` exports `SITE_URL`, `siteTitle/Description(locale)`, `ogLocale(locale)`, `bilingualAlternates(path)` helper that returns `{canonical, languages: {'ar-SD', en, x-default}}`. Root `app/layout.tsx` + `[locale]/layout.tsx` + home + products list + PDP all gained `generateMetadata` with bilingual canonical + hreflang + OG. PDP fetches the product server-side and sets `<title>{name}</title>` + 160-char description + primary image as OG. New `app/sitemap.ts` (revalidate 86_400 — static routes + dynamic from products + categories with per-route alternates) + `app/robots.ts` (disallows `/account`, `/orders`, `/wishlist`, `/checkout`, `/verify-otp`, `/reset-password`, `/design-system`). New `app/opengraph-image.tsx` (root: navy gradient + amber star + Bartal wordmark + AR mark + tagline) and `app/[locale]/products/[id]/opengraph-image.tsx` (per-PDP: same chrome + product name + truncated description + SDG price chip). Both use `runtime = 'edge'`. **`next.config.mjs`** — added `images.formats = ['image/avif', 'image/webp']` + `minimumCacheTTL: 86_400`. **Verified:** `pnpm --filter @bartal/web build` clean — 40 customer routes + `/design-system` + sitemap.xml + robots.txt + 2 OG image routes; `pnpm --filter @bartal/web lint` clean (only pre-existing `<img>` warnings unrelated to this slice). **Key decisions:** (1) **Single bundled PR** for auth + SEO per user preference. (2) **Server Actions** (not Route Handlers) for cookie writes — simpler, type-safe, no manual JSON parsing. (3) **`bartal_user` cookie is non-httpOnly** so client `useAuth()` works without a `/me` roundtrip per render; contains only `{id, name, phone, role, language, is_verified}` — no tokens, no sensitive PII; XSS already gives access to authenticated fetch, so this isn't a new surface. (4) **Refresh-in-middleware** instead of client interceptor — handles 95% of cases on navigation; avoids the refresh-token coordination bug surface in client fetches. (5) **Cart sync is one-way** (local → server on auth success); after that, server cart is authoritative. (6) **Backend-side validation is the source of truth**; client renders `error.message_<locale>` from the envelope rather than re-encoding rules. (7) **No vitest specs this slice** — the web package has no Vitest config yet, and the cookie helper + actions are thin wrappers over already-tested backend endpoints; e2e validation lives in `apps/api/test/auth.e2e-spec.ts`. (8) **No `useFormState`** — plain client `onSubmit` with `useState` for pending/error is simpler and matches the design closer. **What this unblocks:** Phase 6 admin UI now has the same auth primitives to copy. Phase 4 mobile can mirror the cookie / state shape via `flutter_secure_storage`. Phase 5 is done.
- **2026-05-21T15:44:50Z** — Backend Phase 2 / **`admin` module shipped — Phase 2 backend is now 12/12 complete.** 17 endpoints filled at `apps/api/src/modules/admin/admin.service.ts` (~720 lines); thin DTO/wiring updates to `admin.controller.ts` (FileInterceptor on `products/:id/images` + `@CurrentUser` on every mutation handler for audit attribution); `admin.module.ts` imports `OrdersModule` so admin can delegate state transitions to `OrdersService`. **Schema migration:** added `AuditLog` model (`actor_id, entity_type, entity_id, action, before?, after?, created_at`, indexed by actor + by entity-tuple) + `User.audit_logs` back-relation. Migration `20260521151423_add_audit_log` applied locally. **New methods on `OrdersService`:** `adminUpdateStatus` (state-machine guard via existing `canTransition` + `ADMIN_BLOCKED_TRANSITIONS` set covering `RECEIPT_UPLOADED → PAYMENT_CONFIRMED/REJECTED` which must use the dedicated payment endpoint; stock restore on admin cancel; fires `ORDER_SHIPPED/DELIVERED/CANCELLED` SMS via the new `EVENT_FOR_ADMIN_STATUS` map; `TIMESTAMP_FIELD_BY_STATUS` map sets `shipped_at`/`delivered_at`/`cancelled_at`); `adminConfirmPayment` (single transition `RECEIPT_UPLOADED → PAYMENT_CONFIRMED` + `payment_status=PAID` + `paid_at=now`; does NOT auto-cascade to PROCESSING per PRD §11.4); `adminRejectPayment` (requires trimmed reason ≥3 chars; stores reason in history note; fires `PAYMENT_REJECTED` SMS with reason in context). `fireOrderSms` signature extended with optional `reason` arg. **State machine extension:** `PROCESSING/SHIPPED → CANCELLED` added to `state-machine.ts` (PRD §11.3 permits admin cancellation from these states). **SMS templates:** 4 new events appended to `orders/helpers/sms-templates.ts` (`PAYMENT_CONFIRMED`, `PAYMENT_REJECTED`, `ORDER_SHIPPED`, `ORDER_DELIVERED`) with AR + EN copy ≤140 chars; `OrderSmsContext` gained optional `reason`. **Audit:** every admin mutation writes a row via a private `writeAudit` helper (try/catch wrapped; never blocks the response). Settings uses a **single summary audit row** per "save all" (not per-key) to avoid log explosion; per-key diff is recoverable from `before`/`after` JSON. **25 new unit tests** in `admin.service.spec.ts` (mocking Prisma + OrdersService + StorageService) covering dashboard / sales analytics range guards / top-products clamp / orders list filters + invalid status / status transition delegation + audit / payment PAID delegation + REJECTED w/ and w/o reason / product CRUD + P2002 slug → 409 + missing category → 404 / product image upload + primary flip / category cycle detection / customer list + groupBy last_order_at / customer 404 / zone ETA validation + audit / settings batch upsert + invalid key + empty rejection. **8 new orders.service.spec.ts tests** for the admin methods (happy paths + INVALID_ADMIN_TRANSITION + INVALID_STATUS_TRANSITION + REJECTION_REASON_REQUIRED + ORDER_NOT_FOUND + stock restore on admin cancel). **New e2e** at `apps/api/test/admin.e2e-spec.ts` clones the orders skeleton: admin logs in via seeded credentials (`+249912000001` / `ChangeMe123!`); customer registers + verifies + creates BANK_TRANSFER order + uploads receipt; admin confirms payment + advances to PROCESSING + advances to SHIPPED (asserts the AR SMS via the order_number substring); admin lists customers and finds the buyer with `order_count=1`; admin updates ZONE_C fee then restores it; admin batches settings and reads them back; non-admin gets 403 FORBIDDEN. Full suite **167 unit + 9 e2e = 176 tests passing across 17 suites** (was 141 + 8 = 149). **Pre-existing DTO bug fixed in passing:** several admin DTO numeric fields (`fee`, `free_above`, `price`, `compare_price`) had only `@Type(() => Number)` with no class-validator decorator; global `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })` was stripping them, which only surfaced when the field was required. Added `@IsNumber() @Min(0)` to those. Same for `UpdateSettingsDto.settings` (added `@IsObject() @IsNotEmpty()`). **Key decisions:** (1) **No CSV bulk import** — not in PRD §7.2 / §10.3, deferred. (2) **No admin reviews moderation** — `is_published` / `flagged_reason` columns + endpoints come with a follow-up reviews-moderation slice. (3) **Audit writes are inline, not middleware** — keeps `before`/`after` snapshots accurate (only the handler knows prior state) + matches the codebase's existing inline-side-effect pattern (`fireOrderSms`, `safeClearCart`). (4) **`adminConfirmPayment` does NOT auto-cascade to PROCESSING** — PRD §11.4 keeps these as two explicit steps; easy to flip later. (5) **No `GET /admin/audit-logs` endpoint yet** — model is written but read surface lands with the admin frontend. (6) **Single summary audit row per settings save** — explained above. (7) **AdminService queries Prisma directly for products/categories/customers/zones/settings** rather than importing those modules' read services — admin writes are curated for the admin view (no filtering by is_active, full row returned) and the read services filter for the customer view. **Phase 2 backend is complete.** Next slice candidates: (a) web auth glue (swap demo state to real `/api/auth/*` + cookies + cart sync + receipt upload wiring) — first slice that exposes the entire backend to real customers; (b) web SEO + perf polish; (c) Phase 4 mobile (Flutter) scaffold; (d) Phase 6 admin dashboard UI (now unblocked by every endpoint it needs being live); (e) Phase 7 infra (Dockerfiles, nginx, CI/CD, observability).
- **2026-05-21T14:19:10Z** — Backend Phase 2 / `reviews` module extracted + implemented end-to-end. Created `apps/api/src/modules/reviews/` with `reviews.module.ts` + `reviews.controller.ts` (`@Controller('products')` declaring `:id/reviews` so PRD URL paths stay identical) + `reviews.service.ts` (~160 lines) + `dto/reviews.dto.ts` (`CreateReviewDto`, `ListReviewsQueryDto` with `sort ∈ {newest, highest, lowest}`). Removed the 2 review routes from `products.controller.ts`, `listReviews`+`createReview` from `products.service.ts`, and `CreateReviewDto` from `products.dto.ts` — ProductsService is now pure products. Wired `ReviewsModule` into `AppModule` (one-line addition). **`list` flow:** confirm product exists + active → `$transaction([count, findMany, groupBy])`; computes `summary = { count, average_rating (rounded to 1 dp, null when count=0), distribution: { 1..5 } }`; pagination clamp page≥1 / limit∈[1,50]. **`create` flow:** verify user active → verify product active → verified-purchase lookup via `prisma.order.findFirst({ user_id, status: 'DELIVERED', items: { some: { product_id } } })` → insert with `is_verified_purchase: true` → on Prisma `P2002` (unique constraint on `(product_id, user_id)`) rethrow as 409 `REVIEW_ALREADY_EXISTS`; refetch and return `ReviewView`. **10 new unit tests** in `reviews.service.spec.ts` mocking PrismaService — list happy + clamp + 404 + sort=highest + zero-state summary; create happy + USER_NOT_FOUND + PRODUCT_NOT_FOUND + NOT_A_BUYER + REVIEW_ALREADY_EXISTS (P2002). **2 new e2e tests** in `apps/api/test/reviews.e2e-spec.ts` (clones the orders e2e skeleton): buyer seeded with a directly-inserted DELIVERED order + OrderItem → POST review (201) + GET shows summary 5/5 + duplicate POST (409); stranger (no delivered order) → 403 NOT_A_BUYER. Full suite **133 unit + 8 e2e = 141 tests passing across 11+4 suites** (was 123+6 = 129). **Key decisions:** (1) **Schema unchanged** — no `title` / `tags` / `photo_keys` / `is_anonymous` / `is_published` columns. PRD §10.3 + §7.1.5 don't require them; web design's WriteReview extras land in a follow-up slice once storage-module review-photo upload is built. (2) **Auto-publish** — no moderation queue this slice; the admin module will add `is_published` + `flagged_reason` columns + `PUT /admin/reviews/:id/{approve,reject}` when it lands. (3) **No cross-module imports** — reviews service queries `prisma.order.findFirst` directly with `status: 'DELIVERED' + items.some` filter; avoids `OrdersService` cross-import and any circular-module risk. (4) **`Product.rating_avg` / `Product.reviews_count` not denormalized** — `groupBy` on each list call is cheap with the `@@index([product_id, rating])` already on the table; revisit if product-listing latency regresses. (5) **`average_rating` rounded to 1 decimal place** (e.g., 4.7) for UI; clients can format further. (6) **Response shapes (`ReviewView`, `PaginatedReviews`, `ReviewSummary`) co-located** in the service rather than promoted to `@bartal/shared` — promote when both web and admin consume them. (7) **Prisma groupBy typing fix:** initially used `_count: { _all: true }` which Prisma's generated types can't narrow; switched to `_count: { rating: true }` + added required `orderBy: { rating: 'desc' }` (groupBy requires orderBy when aggregating by a non-by field). What this unblocks: the web's `/orders/:id/review` page can call `POST /api/products/:id/reviews` once the auth-glue slice lands; product detail pages can display verified-purchase reviews + summary; admin module's moderation endpoint just needs to add columns + routes (the service is ready to accept a `where: { is_published: true }` filter when the column exists). **Next:** admin module is the last open backend slice — 19 endpoints (dashboard KPIs, orders/products/customers/categories/zones/settings CRUD, CSV import, audit_log). After admin: web auth glue + SEO/perf polish to close out Phase 5.
- **2026-05-20T22:44:28Z** — Backend Phase 2 / `notifications` module productionized end-to-end. `apps/api/src/modules/notifications/notifications.service.ts` rewritten (~250 lines) to actually call Africa's Talking + Firebase. **AT path:** raw `fetch` POST to `https://api.{sandbox.,}africastalking.com/version1/messaging`, `apiKey` header (no Basic auth), `URLSearchParams` body, 10s `AbortController` timeout. PENDING row written before the call, updated to SENT (with `provider_ref = messageId`) or FAILED (`error_message = '${statusCode}: ${status}'` or `'HTTP ${status}: ...'` or `'TIMEOUT_10S'`). Cost goes to logger on success — no `SmsLog.cost` column this slice. **FCM path:** `firebase-admin@^12.7.0`, lazy + safe init in `onModuleInit` reading the service-account JSON from `FCM_SERVICE_ACCOUNT_PATH`; any failure (no path / missing file / bad JSON / init throw) flips `fcmStubMode = true` permanently and logs a warning. `sendPush` calls `admin.messaging().send`; on `messaging/registration-token-not-registered` or `invalid-registration-token` it clears the dead token from `User.fcm_token` via `prisma.user.updateMany` (best-effort, swallowed on failure). **Neither method throws** — failures land in logs / sms_logs only, so auth + orders callers (already wrapped in try/catch) keep their fire-and-forget contract. New `notifications.constants.ts` exports the `SMS_LOG_STATUS` set, `AT_TIMEOUT_MS`, and the success-status set `{101, 102}`. **New endpoint `PUT /users/me/fcm-token`** on `UsersController` accepts `{ fcm_token: string | null }` (MaxLength 4096, null skips length check); `UsersService.updateFcmToken` writes via Prisma with a `select: { fcm_token: true }` projection and throws `USER_NOT_FOUND` for inactive accounts. **New deps:** `firebase-admin@^12.7.0` only; SMS uses Node 20 built-in fetch. Approved `protobufjs` + `sharp` build scripts in `pnpm-workspace.yaml` `allowBuilds` (pnpm 11 now requires explicit approval). **13 new unit tests** in `notifications.service.spec.ts` (mocks `firebase-admin` via factory; mocks `node:fs` via factory because spying on node:fs throws "Cannot redefine property" — switched from `jest.spyOn(fs, ...)` to `jest.mock('node:fs', ...)` + cast `as jest.Mocked<typeof fs>`; mocks `global.fetch` per test). Cases: SMS stub mode no-HTTP, live success on sandbox + prod URLs, AT 403/InvalidPhoneNumber → FAILED with statusCode prefix, HTTP 401, HTTP 5xx, AbortError → TIMEOUT_10S, Arabic body URL-encoded (asserts `%D8|%D9` octets), FCM stub no-op, FCM happy path, FCM token-revoked clears column. **3 new tests** in `users.service.spec.ts` covering set/clear/inactive-user-rejected for `updateFcmToken`. **1 new e2e test** in `auth.e2e-spec.ts` for the new endpoint (set + clear + assert `sms_logs` has a STUBBED row from the register/verify path). Full suite **123 unit + 6 e2e = 129 tests passing across 10+3 suites**. **Pre-existing bug fixed in passing:** pnpm-workspace.yaml `allowBuilds` had placeholder strings ("set this to true or false") for protobufjs/sharp, which pnpm 11 treats as `ERR_PNPM_IGNORED_BUILDS`; replaced with `true`. **What this unblocks:** auth OTP SMS will actually reach customer phones in production once `AT_API_KEY` is set; order-event SMS likewise (admin-driven transitions will fire when admin module wires those endpoints — templates are ready); mobile + web clients can register their FCM token after sign-in. Next backend module candidates: `reviews` (verified-purchase now real) or `admin` (largest, 19 endpoints).
- **2026-05-20T22:21:25Z** — Backend Phase 2 / `storage` module implemented end-to-end. 3 endpoints filled at `apps/api/src/modules/storage/storage.service.ts` (~230 lines) + new `storage.controller.ts` + `dto/storage.dto.ts` + `helpers/keys.ts` (productKey/receiptKey builders + `imageMimeFilter` for Multer). New deps: `@aws-sdk/client-s3@^3.1050`, `@aws-sdk/s3-request-presigner@^3.1050`, `sharp@^0.33.5`, `@types/multer@^1.4.13`. Sharp pipeline: EXIF-rotate → width-clamp 1200px (`withoutEnlargement`) → WebP q80 → if >200KB re-encode at q60 → if still >200KB hard-reject with `IMAGE_BUDGET_EXCEEDED`. **13 new unit tests** in `storage.service.spec.ts` covering stub-mode init, valid PNG happy-path, empty/missing file (`IMAGE_REQUIRED`), corrupt buffer (`IMAGE_PROCESSING_FAILED`), wide-image resize, q80→q60 fallback exceeded path, ownership-safe receipt lookup (ORDER_NOT_FOUND, INVALID_PAYMENT_METHOD for COD, INVALID_STATUS_TRANSITION for DELIVERED), signed-URL happy path + stub-key acceptance + invalid-prefix rejection. **2 new e2e tests** in `apps/api/test/storage.e2e-spec.ts` running against real DB in stub mode: customer uploads a PNG to `/storage/receipts` → returned key plugs into `POST /orders/:id/receipt`; foreign-order rejection (404); admin signs URL (201); non-admin gets 403. Full test suite **109 unit + 5 e2e = 114 passing across 9+3 suites** (was 96/0). **Key decisions:** (1) **Server-side multipart + sharp** (not client-side presigned PUT) per user scope decision — enforces PRD §16.4 image budget regardless of client honesty; cost is API memory per concurrent upload (10MB cap), acceptable for MVP. (2) **Strict separation** between storage upload + orders state transition: storage returns a key, client makes a second call to `POST /orders/:id/receipt` to actually flip the order status. Keeps storage a leaf module — no `OrdersService` import or circular-dep risk. (3) **Ownership + state guards live in StorageService.uploadReceiptForUser** via `prisma.order.findFirst({ id, user_id, payment_method: BANK_TRANSFER, status ∈ {AWAITING_PAYMENT, PAYMENT_REJECTED} })`. Same error codes as `OrdersService.uploadReceipt` so the two endpoints behave identically. (4) **Stub-mode** when `R2_ACCESS_KEY_ID` empty: sharp still runs (so malformed uploads still 400) but PUT is skipped and keys are prefixed `stub/`. CI passes without R2 creds. (5) **`Order.receipt_url` repurposed as a key**, not a URL — relaxed `UploadReceiptDto.receipt_url` from `@IsUrl()` to a `Matches(/^(receipts\/|stub\/receipts\/|stub:\/\/|https?:\/\/)/)` so it accepts both shapes; `// TODO(schema)` comment for the future column rename. (6) **Signed-URL endpoint is POST** because R2 keys contain slashes; `SignedUrlBodyDto` enforces a `^receipts\/|^stub\/receipts\/` prefix so admins can't accidentally sign keys outside the receipt bucket. (7) **Module stays `@Global()`** even with a controller — Nest globality affects only provider exports. **Pre-existing fixes (latent, not caused by this slice):** `Category.display_order` → `sort_order` in `orders.e2e-spec.ts` (yesterday's slice; never ran e2e because Docker was off) and `storage.e2e-spec.ts`; `auth.e2e-spec.ts` line 117 expected `{ data: { success: true } }` but the actual API returns `{ success: true }` (interceptor passes pre-shaped envelopes through without re-wrap). What this unblocks: the web's `/orders/[id]/receipt-upload` dropzone can call `POST /api/storage/receipts` (two-step: upload, then `POST /api/orders/:id/receipt` with the returned key) once auth glue lands; admin product-image upload in Phase 6 has its primitives ready. Next backend module candidates: `notifications` (productionize AT adapter), `reviews` (verified-purchase now possible since orders are real), or `admin` (largest remaining piece; 19 endpoints).
- **2026-05-20T21:58:47Z** — Backend Phase 2 / `orders` module implemented end-to-end. All 5 endpoints filled in at `apps/api/src/modules/orders/orders.service.ts` (~450 lines): `list`, `detail`, `create`, `cancel`, `uploadReceipt`. Three pure helpers under `orders/helpers/`: `state-machine.ts` (PRD §11.3 transition table + `cancellableStatuses` / `receiptUploadableStatuses` sets), `totals.ts` (`computeOrderTotals`, `lineTotal`, `priceToNumber` — `Prisma.Decimal` at DB boundary, `number` at response boundary), `sms-templates.ts` (4 bilingual events: ORDER_CREATED_BANK / ORDER_CREATED_COD / RECEIPT_RECEIVED / ORDER_CANCELLED). `OrdersModule` now imports `DeliveryModule`. **18 new Jest unit tests** in `orders.service.spec.ts` covering list pagination + clamp, detail ownership, create happy-path for BANK_TRANSFER (status=AWAITING_PAYMENT, stock decremented, cart cleared, SMS fired) and CASH_ON_DELIVERY (status=PENDING), ADDRESS_NOT_FOUND, USER_NOT_FOUND for inactive user, PRODUCT_NOT_FOUND, OUT_OF_STOCK, SMS-failure-isolation, P2002 retry, cancel happy + INVALID_STATUS_TRANSITION (SHIPPED) + ORDER_NOT_FOUND, uploadReceipt happy + INVALID_PAYMENT_METHOD (COD) + INVALID_STATUS_TRANSITION (DELIVERED). Full api test suite now **96 / 96 passing across 8 suites**. e2e test at `apps/api/test/orders.e2e-spec.ts` (register → verify-otp → POST address → seed product → POST /orders → list → detail → POST receipt → reject replay) — runs against real DB via `pnpm db:up && pnpm --filter @bartal/api test:e2e`. **Key decisions:** (1) **Initial status by payment method** — BANK_TRANSFER → `AWAITING_PAYMENT` (waiting on receipt), CASH_ON_DELIVERY → `PENDING` (waiting on admin approval). (2) **Stock decrement at create-time** (no PAYMENT_CONFIRMED wait) with `TODO(concurrency)` comment — accepted MVP oversell risk; mitigation deferred to conditional `updateMany({ where: { id, stock: { gte: qty } } })`. (3) **Order-number collision** handled by `@unique` constraint + one-shot `P2002` retry inside `create()` — keeps the existing count-based `nextOrderNumber` helper without a new AppSetting hot row. (4) **Interactive `$transaction(async tx => ...)`** for create + cancel because the order id is needed for the OrderStatusHistory insert; tests mock `$transaction` to accept both array and function forms. (5) **Post-commit side-effects** (`redis.del`, `notifications.sendSms`) are wrapped in try/catch — a paid-up order survives a transient SMS/Redis hiccup; verified by a dedicated unit test that rejects SMS but still expects create to resolve. (6) **No address snapshot on Order** — schema only has `address_id` FK, detail response joins via `include`. (7) **`Order.notes` populated from DTO**; `internal_notes` reserved for admin. (8) **Receipt upload accepts URL only**; real R2 presign + sharp WebP pipeline is deferred to the `storage` module per user decision. (9) **No admin/refund/audit endpoints** — explicitly out of scope; tracked separately in this file. (10) **Response shapes (`OrderView`, `PaginatedOrders`) co-located in the service** rather than promoted to `@bartal/shared` until admin / web need them. What this unblocks: the web's `useOrders` Zustand store can swap demo data for real `GET /orders` + `/orders/:id` calls (one-line change per call site) once the auth glue lands; the receipt-upload dropzone on `/orders/:id/receipt-upload` can wire to `POST /orders/:id/receipt` directly with a URL placeholder until R2 presign exists. Next backend module candidates: `notifications` (productionize the AT adapter), `storage` (R2 presign + sharp), `reviews` (now has verified-purchase via delivered orders). Admin module is the largest remaining piece (19 endpoints) and depends on orders/products/users being done — those prereqs are now met.
- **2026-05-15T10:14:00Z** — Repo bootstrapped: monorepo scaffold begun.
- **2026-05-15T17:24:42Z** — Phase 1–3 scaffold complete: `@bartal/shared` builds clean, NestJS API compiles to `dist/main.js`, Prisma client generated against the full PRD §9 schema, seed script ready, all PRD §10.3 routes exposed (stubbed with `NotImplementedException`). `pnpm install` succeeded with 692 packages.
- **2026-05-15T17:32:00Z** — Phase 3 closed: Postgres + Redis up on ports 5433/6380 (defaults taken by another local project), `prisma migrate dev --name init` applied, seed inserted 1 admin + 4 delivery zones + 7 app settings + 6 categories + 20 sample Sudan-market products.
- **2026-05-15T17:55:00Z** — Phase 2 partial: implemented `products`, `categories`, `delivery` services against Prisma with full unit tests (18 tests, 4 suites). Tightened `SuccessResponseInterceptor` to hoist `{data, meta}` into the PRD §10.2 paginated envelope shape.
- **2026-05-15T20:38:59Z** — Phase 5 website live: pulled the Claude design bundle into `docs/design/bartal/`, translated `tokens.jsx` into Tailwind preset + CSS variables, scaffolded Next.js 14 with `[locale]` routing + middleware-driven SSR `lang`/`dir`, built header/footer/product card/price tag/logo/motif components, wired axios + React Query to fetch real seed data, implemented home/products/product-detail/cart/checkout/login/register/account/orders/wishlist/search/404. `pnpm build` produces 18 routes; `GET /ar` returns RTL Arabic homepage with seeded products, `GET /en` returns LTR English equivalents, `GET /ar/products/karkadeh` returns full PDP with description and add-to-cart.
- **2026-05-15T19:04:37Z** — Expanded `tasks.md`: added top-of-file Snapshot panel, exploded every open phase into endpoint- / screen- / page-level sub-tasks with PRD section refs and `docs/design/bartal/project/*.jsx` source pointers, added acceptance criteria per module group. No code or schema changes.
- **2026-05-15T19:39:53Z** — Web Phase 5 / checkout step 1 shipped: new route `/[locale]/checkout/address` ports `web-checkout-steps.jsx::WebCheckoutAddress` (address radio cards + 4-zone grid + landmark notice + navy continue CTA). Added `checkout-store` (Zustand, ephemeral) seeded with two demo addresses, plus shared scaffolding (`CheckoutShell`, `CheckoutStepper`, `OrderSummarySidebar`) used by steps 2–3 next. Banner on legacy `/checkout` links to the new flow. Existing `/checkout` preview kept until step 3 ships. Verified: `pnpm build` → 20 routes; AR + EN rendered in Playwright matching the design (RTL Cairo / LTR Poppins, Zone B default, Home address default, sidebar updates live when zone/address changes); RTL audit (zero directional Tailwind utilities) and hex-code audit (zero hard-coded hex values) both clean. Side-effect: copied root `.env` → `apps/api/.env` so Nest's ConfigModule can boot the API in dev — known gotcha, not a code change.
- **2026-05-15T20:21:53Z** — Web Phase 5 / checkout step 2 shipped: new route `/[locale]/checkout/bank` ports `web-checkout-steps.jsx::WebCheckoutBank` (3 bank radio cards, expanded copy panel with Account name / Account # / SWIFT / Amount / Reference rows, "After you transfer" sand-bg ordered list, navy continue CTA + back link). New components: `BankRadioCard`, `CopyableRow` (with `navigator.clipboard.writeText` + 1.4s "Copied" feedback), `AfterTransferNotice`. Added `BankIcon` + `CopyIcon` to `Icons.tsx`. Banks hardcoded in the page (Faisal Islamic / Omdurman National / Bank of Khartoum — demo data; no banks API yet). Amount formatted Latin-only (`fmtSDG(total, 'en')`) per design — Sudanese banking apps expect ASCII numerals even on AR pages. Reference `BRT-2026-PREVIEW` (real order numbers come with `orders` API). Verified: `pnpm build` → 22 routes; AR + EN screenshots match the design pixel-for-pixel (stepper shows step 1 past/amber-tint, step 2 active/amber; Faisal selected by default with Recommended pill; expanded panel with 5 dashed-underline copy rows; copy button color amber). RTL + hex audits clean. Note: had to wipe `.next/` and restart dev once because running `pnpm build` clobbered the dev server's vendor chunks.
- **2026-05-15T20:43:55Z** — Web Phase 5 / checkout step 3 shipped: new route `/[locale]/checkout/review` ports `web-checkout-steps.jsx::WebCheckoutReview` (3 edit-back cards SHIP TO / PAYMENT / ITEMS, terms checkbox, navy "Place order & continue to receipt upload" CTA, back link). New components: `ReviewEditCard`, `ReviewItemsList`, `TermsAgreementRow`. Place Order routes to synthetic `/checkout/thank-you/BRT-2026-PREVIEW` (404 expected — page 4 next slice). Edit links route to `/checkout/address`, `/checkout/bank`, `/cart` per design. Shared scaffolding: extracted the duplicated subtotal/delivery-fee/total calc into `lib/state/use-order-total.ts` hook and refactored `OrderSummarySidebar` + `CheckoutBankStep` to consume it (no behavior change). Moved the BANKS constant from `CheckoutBankStep` to `lib/state/checkout-banks.ts` with `findBankById` + `PREVIEW_ORDER_REFERENCE` exports so the review page can look up the selected bank for the masked tail display. SKU on items synthesized as `SKU BRT-{slug.toUpperCase()}` since `CartLine` has no SKU field. Verified: `pnpm build` → 24 routes; AR + EN screenshots match the design (RTL Cairo + LTR Poppins, all 3 cards present with amber edit links, payment card resolves live state — AR session showed Omdurman tail "8800" because the bank-step toggle persisted across nav, EN session showed Faisal default "9000"; terms default-checked; CTA disabled state confirmed when cart empty). RTL audit + hex audit zero matches across new `review/` files and new `components/checkout/Review*` + `TermsAgreementRow`. Hiccup: a stale `.next/` (left by the prior dev server) caused EINVAL readlink during the rebuild — wiped + restarted dev.
- **2026-05-16T23:24:47Z** — Backend Phase 2 / `cart` module implemented end-to-end. All 5 endpoints in `apps/api/src/modules/cart/cart.service.ts` (~270 lines): `get`, `addItem`, `updateItem`, `removeItem`, `clear`. Redis-backed via existing `RedisService` (`bartal:cart:<userId>` keys, 30-day TTL on every write). Stored shape is minimal (`{product_id, quantity}[]`); the full `CartView` is recomputed on every read by joining against the live `Product` table — so prices, stock, and active-status stay accurate. **16 new Jest unit tests** in `cart.service.spec.ts` covering: empty cart (Redis miss), happy paths for all 5 methods, the self-heal write-back when inactive products are dropped, full-empty self-heal via `redis.del`, corrupt-payload reset, stock-cap on merge, ITEM_NOT_IN_CART on update, PRODUCT_NOT_FOUND for missing/inactive products, idempotent remove. Full api test suite now **78 / 78 passing across 7 suites**. **Key decisions:** (1) Redis is the only store; the unused `CartSession` Prisma model is left for future write-through hardening — not needed in v1. (2) Self-heal pattern: when the read drops inactive products or clamps quantities to current stock, it writes the cleaned items back to Redis (or `del` if the cart is now empty). (3) `attachDeliveryPreview` looks up the user's default address (`prisma.address.findFirst({ user_id, is_default: true })`) and calls `DeliveryService.calculateFee(zone, subtotal)`. No default address → `delivery_preview: null` + `requires_address: true` so the web prompts the user. (4) `DeliveryModule` already exported `DeliveryService` — only `CartModule.imports` needed updating. (5) `Prisma.Decimal` → JS number via `.toNumber()` helper for `price`; SDG amounts are well within safe-integer range. (6) `variant_id` on `AddCartItemDto` is accepted but ignored — Product schema has no variants wired in v1. What this unblocks: the web's `useCart` Zustand store can swap localStorage for `GET/POST/PUT/DELETE /cart` calls (one-line change per call site once auth glue is in). More importantly, this is a prerequisite for `orders` — `POST /orders` will snapshot the cart, decrement stock, and clear the cart in one transaction. Next natural module: `orders` (largest — integrates auth + users + cart + delivery + storage).
- **2026-05-16T23:14:34Z** — Backend Phase 2 / `users` module implemented end-to-end. All 9 endpoints filled in at `apps/api/src/modules/users/users.service.ts` (~230 lines): `getProfile`, `updateProfile`, `changePassword`, `deleteAccount`, `listAddresses`, `createAddress`, `updateAddress`, `deleteAddress`, `setDefaultAddress`. Returns bilingual error envelopes (`USER_NOT_FOUND`, `EMAIL_EXISTS`, `INVALID_CURRENT_PASSWORD`, `ADDRESS_NOT_FOUND`, `ADDRESS_HAS_ORDERS`). **20 new Jest unit tests** at `users.service.spec.ts` (mocked Prisma + password util) covering happy paths + key error branches: email-collision rejection (and same-user no-op), wrong-current-password, soft-delete + token revocation, address ownership 404 on every owner-checking method, P2003 FK error rethrow as 409 ADDRESS_HAS_ORDERS, first-address auto-promotion to default, transaction-wrapped default flips. Full api test suite now **62 / 62 passing across 6 suites**. **Key decisions:** (1) Reuses `PublicUser` interface exported from `auth.service.ts` — no shared types file needed yet. (2) `changePassword` + `resetPassword` + `deleteAccount` all revoke ALL refresh tokens (same `updateMany` pattern as `AuthService.logout`). (3) Default-address logic is centralized: createAddress with `is_default:true` and the user has prior addresses, updateAddress toggling to default, and setDefaultAddress all run an `updateMany({ where: { user_id, is_default: true, id: { not: target } }, data: { is_default: false } })` inside `$transaction` so there's never a window with 0 or 2 defaults. (4) Address ownership checks use `findFirst({ id, user_id })` (single query) not `findUnique({ id })` + code-side check — avoids timing-based id-existence leak. (5) Address deletion does NOT auto-promote another default — simpler than guessing intent; the user picks one next checkout. (6) Address with linked Orders catches Prisma `P2003` and rethrows as friendly 409 `ADDRESS_HAS_ORDERS` (Order.address_id has no cascade per schema). What this unblocks: the web's `useAccount` + `useAddresses` Zustand stores can swap demo data for real `GET /users/me` + `/me/addresses` calls (one-line change per call site) once the auth glue lands; the checkout address picker reads from the same `useAddresses` so it benefits automatically. Next backend module candidates: `cart` (Redis-backed, smallest) → `orders` (largest, integrates auth + users + cart + delivery + storage) → `reviews`.
- **2026-05-16T21:46:37Z** — Backend Phase 2 / `auth` module implemented end-to-end. All 8 endpoints filled in at `apps/api/src/modules/auth/auth.service.ts` (~280 lines): `register`, `verifyOtp`, `resendOtp`, `login`, `refresh`, `logout`, `forgotPassword`, `resetPassword`. Returns bilingual error envelopes via thrown `UnauthorizedException`/`ConflictException`/`ThrottlerException` with `{ code, message_en, message_ar }` bodies (`GlobalExceptionFilter` already maps these). Helpers: `apps/api/src/modules/auth/helpers/tokens.ts` (`generateRefreshToken` = 48-byte base64url random, `hashRefreshToken` = sha256 for deterministic indexed lookup, `generateOtpCode` = 6-digit zero-padded, `parseTtlSeconds` for "15m"/"30d" → seconds) and `helpers/messages.ts` (bilingual SMS templates per `OtpPurpose × Language`). Refresh tokens are opaque random secrets (not JWTs) stored hashed; rotation uses an atomic `updateMany({ id, revoked: false })` CAS — concurrent refresh requests with the same token result in only one succeeding, the other gets `INVALID_REFRESH_TOKEN`. **24 new Jest unit tests** at `auth.service.spec.ts` (mocked Prisma + Jwt + Notifications) covering happy paths and key error branches for all 8 methods: existing-verified-user → CONFLICT, unverified-existing reuses row, OTP invalid/expired, wrong password, unverified login, inactive user, all 4 refresh-token rejection modes (missing/expired/revoked/CAS race). Full api test suite now **42 / 42 passing across 5 suites**. **e2e test** at `test/auth.e2e-spec.ts` written (register → fetch OTP from DB → verify → login → refresh → replay-fails → logout → revoked-after-logout) — compiles cleanly but couldn't run this session because Docker (Postgres :5433) was off; run with `pnpm db:up && pnpm --filter @bartal/api test:e2e`. `NotificationsService.sendSms` in dev (no `AT_API_KEY`) logs the OTP message and persists to `sms_logs` with status `STUBBED` — e2e flow reads OTPs straight from the DB. Module wiring needed no changes: `PrismaModule` and `NotificationsModule` are both `@Global`. Service exports `PublicUser` and `TokenPair` interfaces so the controller's inferred return types resolve (TS4053 fix). What this unblocks: the 4 "Auth glue" web tasks (real `POST /auth/login` swap, real OTP screen, forgot/reset) plus the next backend modules (`users`, `cart`, `orders`, `reviews`) that depend on authenticated users.
- **2026-05-16T17:46:18Z** — Web Phase 5 / final content pages shipped — `/journal` + `/brand`. **Customer-facing visual surface now complete (38/38).** New routes: `/[locale]/journal` (featured post hero, 6-category client-side filter, 3-col post grid, navy newsletter strip with `MotifBg` overlay + client-side email validation + success state) and `/[locale]/brand` (top breadcrumb strip, navy hero band with motif + 120×120 initial avatar + verified pill + follow/share buttons, 2-col about + 4-row trust metrics card, 4-tab navigation, 6-product grid with `−%` discount ribbons via `PriceTag compare`). 4 new components under `apps/web/src/components/static/`: `JournalFeaturedCard`, `JournalPostCard`, `BrandHeroBand`, `TrustMetricsCard`. Dictionary: added `dict.web.journal.*` (featured post + 6 posts + 6 categories + newsletter strings) and `dict.web.brand.*` (brand data + 6 products + 4 tabs + about + 3 trust metrics + breadcrumb) for both AR + EN — Arabic uses Arabic-Indic numerals throughout (٢٠٢٤, ٤٫٨, ٩٩٫٤٪) per design. Reused existing `WebStaticShell` (Journal hero matches Privacy/About pattern), `MotifBg`, `ProductPlaceholder`, `PriceTag` (with `compare` prop for strikethrough). No new state stores, no new shared types, no API dependency — purely static demo content. Newsletter and Follow buttons are cosmetic per design (no endpoints exist yet); newsletter swaps to a success card on submit, Follow has no handler. Verified: `pnpm --filter @bartal/web build` clean → **40 routes** (was 38). RTL audit + hex audit zero matches across new files. Skipped per plan: Footer links to `/journal` and `/brand` (deferred — Footer already crowded, easier to wire from search/header in a later pass).
- **2026-05-16T16:23:30Z** — Web Phase 5 / Account + Orders + Wishlist slice shipped (11 pages — the entire logged-in customer surface). New routes: `/[locale]/account` (dashboard hub), `/account/profile`, `/account/addresses`, `/account/security`, `/account/preferences`, `/orders` (history), `/orders/[id]` (detail), `/orders/[id]/receipt-upload`, `/orders/[id]/review`, `/orders/[id]/invoice` (A4 print-ready), `/wishlist`. Two new pages added to the design backlog: `/account/preferences` (no design source — constructed from `LanguageToggle` + theme radio cards + notification `ToggleRow`s) and the prior tracked items rounded up to 11 (added profile, preferences to the original 9). Foundation: 4 new types in `@bartal/shared` (`Order`, `OrderItem`, `Address`, `WishlistItem`, `UserProfile`, `PaymentReceipt`, `DemoOrderStatus`); 5 new Zustand+persist stores in `apps/web/src/lib/state/` (`account-store`, `addresses-store`, `orders-store`, `wishlist-store`, `preferences-store`) seeded with realistic demo data (5 orders covering every status pill color, 3 addresses across zones, 4 wishlist items). Refactored `checkout-store.ts` to read addresses from the shared `addresses-store` — checkout + account + orders now share one address source of truth. Shared components: `components/account/{WebAccountLayout,StatCard,VerificationRow,AddressCard,ToggleRow,SessionRow}`, `components/orders/{StatusPill,OrderTimeline,OrdersShell}`, `components/wishlist/WishlistCard`. Dictionary: ~330 new lines spanning `dict.web.account.{nav,dashboard,profileSection,addressesSection,securitySection,preferencesSection}`, `dict.web.orders.{statusPills,history,detail,receiptUpload,review,invoice}`, `dict.web.wishlist` — both AR + EN. Removed `<LoginRequired>` from the 3 ungated stubs (`/account`, `/orders`, `/wishlist`); matches checkout precedent — pages render full UI on demo data, real auth gating waits on `auth` API module (tracked under "Auth glue"). Verified: `pnpm --filter @bartal/web build` clean → **38 routes** (was 32). RTL audit (zero directional Tailwind utilities across new files) and hex audit (only intentional `#E0DBC9` star-inactive color from the design source remains; everything else uses `BARTAL` tokens) both clean. Patched `AddressRadioCard` to use generic label lookup so the new "parents" address renders correctly in checkout. Patched `BartalLogo` prop name (`lang` → `locale`) in the design-system page from a prior session. Skipped this session: the live Playwright screenshot pass — a prior dev server was already bound to :3000 with a stale `.next/`, and starting a competing instance on :3001 failed (Windows EINVAL on shared `.next/` readlink); killing the unknown :3000 process was off-limits per CLAUDE.md safety protocol. The build success + audits are the verification this session; visual screenshots can be captured next session once the orphaned dev server is reset.
- **2026-05-15T23:45:38Z** — Web Phase 5 / static slice page 8 (final, internal-only) shipped: new route `/[locale]/design-system` ports `final-web-and-styletile.jsx::StyleTile`. Gated behind `process.env.NODE_ENV !== 'production'` (`notFound()` in prod) + `robots: { index: false, follow: false }` metadata + visible "INTERNAL · Design-system reference · not exposed to customers in production" navy banner at the top. Single bespoke page, no new shared components — uses existing `MotifBg`, `BartalLogo`, `LogoMark`, `PriceTag`, `ProductPlaceholder`, `BARTAL` tokens. Content: 9 token cards in a 2-col grid — Logo + Motif (navy bg with amber-soft motif overlay, AR+EN logo lockups, 3 mark sizes, etymology), Typography (Cairo/Poppins/JetBrains Mono samples), Palette · brand (9 swatches in 3-col grid), Status + Dark mode (3 status colors + 4 dark surface chips), Buttons (Primary/Accent/Secondary/Ghost + 5 status pills), Price tag · numerals (4 PriceTag variants Latin/Arabic/Small/Plain), Product imagery (6 placeholder hues), Motif · radii (1 motif tile + 6 radius samples 6-999px), and a Surface map showing mobile/web/admin/brand page counts. **Not counted in customer page budget** — adjusted total from `~34` to `~33` customer pages. Customer-facing static slice (Privacy + Terms + FAQ + About + Contact + 404/500/Offline) is **complete at 8/8**.
- **2026-05-15T23:38:03Z** — Web Phase 5 / static slice pages 6-7 shipped (combined since they share one component) — branded 404, 500, offline. New `apps/web/src/components/static/WebSystemState.tsx` (client) takes `kind: '404' | '500' | 'offline' | locale | dict | onRetry?` and renders the big code/glyph + variant-colored "?" / "!" / "×" badge, eyebrow tag, h1 title, body, primary action (Back to homepage for 404, Try again for 500/offline), Contact support secondary, plus a "Try visiting" suggestions card with 4 amber pill links on the 404 variant. Wi-Fi-off SVG glyph for offline. Backdrop motif pattern (100×100 16-point star at 4% opacity in the variant color). Red blinking banner at top for offline only. "ref: RNF/ERR-NNNNNN · bartal.sd" mono debug line at the bottom for 404 & 500 (generated client-side in useEffect to avoid hydration mismatch). Three mount points: (a) `app/[locale]/not-found.tsx` — upgraded the minimal English stub to client component that reads locale from `usePathname()`, renders `WebSystemState kind="404"`; (b) `app/[locale]/error.tsx` — new client error boundary, passes Next's `reset` as the `onRetry` callback so the Try-again button retries the boundary; (c) `app/[locale]/offline/page.tsx` — new server route `/[locale]/offline`. Plus `app/[locale]/[...rest]/page.tsx` catch-all that calls `notFound()` — without it, unmatched URLs got Next's default 404 instead of my branded one. Dictionary: `dict.web.systemState.*` with variants, actions, offline banner, 4 suggestion links with `href` paths, ref label. Verified all 5 routes: `/en/totally-not-a-real-path` and `/ar/totally-not-a-real-path` render the branded 404 with locale-correct copy; `/en/offline` and `/ar/offline` render the offline state with red banner; real routes (about, privacy, products, /checkout/address) still 200 (catch-all only fires for unmatched URLs). Screenshots saved to `.screenshots/static/system-state/{404-en, 404-ar, offline-en, offline-ar}.png`. RTL + hex audits clean.
- **2026-05-15T23:28:21Z** — Web Phase 5 / static slice page 5 shipped: new route `/[locale]/contact` ports `system-kit.jsx::WebContact`. Reuses `WebStaticShell` for the hero. Body is a 2-col `lg:grid-cols-[1.1fr_1fr]` with: (left) new `ContactForm` client component — 3 inputs (Full name / Phone / Order ID optional), 6-pill Topic picker (single-select, "Order issue" default), Message textarea, full-width amber CTA. Client-side validation: name required, phone required and must match `+249 NN NNN NNNN`, message ≥10 chars. On valid submit, the form swaps to a success card with the dictionary's success copy ("our team will reach out via WhatsApp within 30 min" / Arabic equivalent) — no backend yet, all client. (right) Channels rail — WhatsApp card (real `wa.me` link, brand-green icon background, "PREFERRED" pill, hex literal `#25D366` since it's WhatsApp's official color not a Bartal token), Email card (navy ✉ + `mailto:`), Phone card (amber ☏ + `tel:`), Headquarters card with 3-line address. Dictionary: `dict.web.contact.*` with fields, 6 topic labels, validation messages, success copy, 3 channel definitions, office lines array. RTL behavior: grid columns swap so form sits on the right in AR and left in EN; phone numbers stay `dir="ltr"` mono. Footer "Contact" link in `Footer.tsx` was previously hardcoded text — left as-is for now (it's nested in HELP column which isn't a single static link). Both routes return 200. RTL + hex audits clean on new files (the `#25D366` WhatsApp green is in `page.tsx` only and intentional — external brand color).
- **2026-05-15T23:23:01Z** — Web Phase 5 / static slice page 4 shipped: new route `/[locale]/about` ports `system-kit.jsx::WebAbout`. Reuses `WebStaticShell` for the eyebrow/title/subtitle hero, then adds three sections in the body: (1) **brand strip** — 200px navy-ink card with an inline SVG motif pattern (90×90 single 16-point star in amber-soft, 25% opacity — a different motif variant from the `MotifBg` used on the checkout thank-you page; inlined since this is the only place that uses it), centered "bartal" / "برتال" wordmark in 48px white + "From Sudan · for Sudan" amber-soft uppercase tagline; (2) **stats grid** — 4 cards (12,400+ active customers / 180+ trusted sellers / 18 states / 24h Khartoum delivery), JetBrains-Mono amber numbers; (3) **values** — 3 cards with emoji glyphs (🏛 Local first, 🤝 Trust built in, 📱 Works on any network); (4) **founders** — 3 cards with circular initial avatars (AM amber, SE navy, MO green-ok) + name + role. Dictionary: `dict.web.about.*` with brandStrip, stats[4], values.items[3], team.members[3] (each member tagged with an `accent: 'amber' | 'navy' | 'green'` literal that maps to a Tailwind bg class). All copy verbatim from the design. `pnpm` dev hot-reloaded the new route — `/ar/about` and `/en/about` both 200. RTL + hex audits clean (the 90×90 motif SVG uses `BARTAL.amberSoft` from tokens, not a hex literal).
- **2026-05-15T23:13:09Z** — Web Phase 5 / static slice page 3 shipped: new route `/[locale]/faq` ports `web-extras.jsx::WebFaq`. New component `apps/web/src/components/static/Accordion.tsx` — single-open accordion with `aria-expanded` / `aria-controls`, `+` glyph that rotates to `×` when open. Page composition: server `page.tsx` renders h1 + subtitle + 4 category cards (Orders & delivery / Payment & receipts / Returns / Account with article counts) + a client child `FaqSearchAccordion` (the search input + filtered Accordion — `useMemo` filters questions whose `q` or `a` contains the query, case-insensitive). Dictionary: `dict.web.faq.*` with `title`, `subtitle`, `searchPlaceholder`, `noResults`, `articlesLabel: '{count} articles'`, `categories[4]` and `questions[6]` (verbatim from the design for the first FAQ; the remaining 5 got Sudan-specific real answers — 5-15 min verification, COD supported, 7-day returns, zone-based fees 500-1200 SDG, free above 50K SDG, Khartoum/Omdurman/Bahri coverage). Search verified live: typing "cash" filters to a single FAQ. Default-open is the first FAQ. `pnpm build` had compiled successfully in the prior session and dev hot-reloaded the new files on the fly (no .next clobber this time); both `/ar/faq` and `/en/faq` return 200 with full SSR content. RTL + hex audits clean.
- **2026-05-15T23:05:45Z** — Web Phase 5 / static slice page 2 shipped: new route `/[locale]/terms` ports `web-extras.jsx::WebToS`. Different layout from Privacy — uses a `max-w-[1100px] grid lg:grid-cols-[240px_1fr]` with a **sticky `TableOfContents` rail** (the `variant="sticky"` we built with the Privacy page is now in use). Main column: h1 + "Last updated" + first 4 sections (with the design's verbatim placeholder body — identical for sections 1-4, matches the design wireframe which intentionally repeats) + "… continued" italic stub. Sections 5-10 appear in the TOC but have no body yet (`hasBody: false` flag in the dictionary). Dictionary: `dict.web.terms.*` with `sections[10]` (each `{id, title, hasBody}`), `placeholderBody`, `lastUpdated`, `tocHeading`, `continued`. RTL behaviour: grid columns flip naturally — TOC renders on the right in AR (trailing side), left in EN (leading side); same component, two reading directions. `pnpm build` → 30 routes; AR + EN screenshots saved to `.screenshots/static/terms/`. Footer's "Terms of Service" link now resolves (was 404). RTL + hex audits clean. Same `.next` dev/build conflict as before — wiped + restarted dev.
- **2026-05-15T22:59:16Z** — Web Phase 5 / static slice page 1 shipped: new route `/[locale]/privacy` ports `system-kit.jsx::WebPrivacy` — eyebrow + h1 + subtitle hero, amber "in plain language" SummaryBox, 2-col inline TableOfContents, 6 numbered sections rendered from a strongly-typed `sections[]` array in the dictionary (verbatim AR + EN copy from the design). New shared primitives under `apps/web/src/components/static/`: `WebStaticShell`, `SummaryBox`, `TableOfContents` (with `inline | sticky` variants — sticky will be used by ToS next), `StaticSection` (numbered prose with `id` for anchor scroll + `scroll-mt-32` so the sticky header doesn't cover headings). Dictionary: added `dict.web.privacy.*` to both locales (eyebrow, title, subtitle, summary, tocHeading, 6 sections with `{id, title, body}`). Footer's previously inert "Privacy Policy" / "سياسة الخصوصية" span upgraded to a `Link` pointing at the new route. `pnpm build` → 28 routes. AR + EN screenshots saved to `.screenshots/static/privacy/` showing both renders matching the design (RTL Cairo / LTR Poppins, Arabic numerals in section headers "١. المعلومات...", clickable amber TOC items with "→" glyph). RTL audit + hex audit clean.
- **2026-05-15T21:29:15Z** — Web Phase 5 / checkout step 4 shipped — **multi-step checkout slice complete (4/4)**. New dynamic route `/[locale]/checkout/thank-you/[orderId]` ports `order-thanks-and-reviews.jsx::WebOrderConfirm`. Sections: motif-amber-pattern hero (green 80px checkmark + personalised greeting using `useCheckout.selectedAddress.name` + display headline + body + order-number tile with `Intl.DateTimeFormat` locale-aware date); Step 1 amber-tint `BankInstructionsCard` (6 dashed copy rows: Bank / Account name / Account # / IBAN / Amount due in amber / Reference — IBAN added as a new field on `BankInfo`); Step 2 `ReceiptDropzone` (drag/drop + native file picker, 10MB cap, "Selected · Change" state on pick, no upload — pure client UX); order-summary card (real cart lines + Total in amber via `useOrderTotal`); right column with `OrderStatusTimeline` (5 vertical steps, step 1 amber-filled), delivery-address card, and 3-button action stack (navy "Track my order" → `/orders`, outlined "Continue shopping" → `/products`, disabled "Download invoice (PDF)" with download icon). New shared primitives: `components/MotifBg.tsx` (ports `tokens.jsx::MotifBg` — reusable on brand/journal pages later), `Icons.tsx` `CameraIcon` + `DownloadIcon`. `BankInfo.iban` added to all 3 banks. Dictionary: `checkout.thankYou.*` for both locales with all design copy verbatim. Verified: `pnpm build` → 26 routes (dynamic `[orderId]`); AR + EN screenshots saved to `.screenshots/checkout/thank-you/` showing all sections present and matching the design (RTL Cairo + LTR Poppins, Arabic numerals in the date "١٥ مايو ٢٠٢٦ في ١١:٢٦ م", Latin numerals on the bank Amount field, "Bartal E-Commerce Ltd." / "برتال للتجارة الإلكترونية" account-holder copy verbatim per the design's case difference vs the bank-step page). RTL audit + hex audit clean on new files. End-to-end purchase path now works in browser: cart → /checkout/address → /bank → /review → Place Order → /thank-you. What's left for "real" checkout is API plumbing (POST /orders, R2 receipt upload, GET /orders/:id) — small swaps when those modules land.
