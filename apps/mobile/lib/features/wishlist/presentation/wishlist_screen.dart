import 'package:flutter/material.dart';

/// Placeholder — Slice 2 replaces with the wishlist cards + price-drop /
/// out-of-stock badges (`mobile-extras.jsx::WishlistScreen`).
class WishlistScreen extends StatelessWidget {
  const WishlistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Wishlist — Slice 2')));
  }
}
