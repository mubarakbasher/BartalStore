import 'package:flutter/material.dart';

/// Placeholder — Slice 2 replaces with the two-column category browser
/// (`final-additions.jsx::MobileCategoriesScreen`).
class CategoriesScreen extends StatelessWidget {
  const CategoriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Categories — Slice 2')));
  }
}
