import 'package:flutter/material.dart';

/// Placeholder — Slice 1 replaces with the phone entry + channel selector
/// (`auth-gaps.jsx::ForgotPasswordScreen`).
class ForgotPasswordScreen extends StatelessWidget {
  const ForgotPasswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Forgot password — Slice 1')));
  }
}
