import 'package:flutter/material.dart';

/// Placeholder — Slice 1 replaces with the 3-slide carousel
/// (`auth-gaps.jsx::OnboardingScreen`).
class OnboardingScreen extends StatelessWidget {
  const OnboardingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Onboarding — Slice 1')));
  }
}
