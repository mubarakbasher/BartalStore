/**
 * Bartal design tokens — re-exported from @bartal/shared so the web and admin
 * surfaces stay in lock-step. Source of truth lives at
 * `packages/shared/src/design/tokens.ts`.
 */
export { BARTAL, fmtSDG, PLACEHOLDER_PALETTES } from '@bartal/shared';
export type { Locale, PlaceholderHue } from '@bartal/shared';
